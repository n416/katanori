# yorisoi-care-app × カタノリロボ 統合仕様書

## 1. 概要
本設計は、自律型対話WEBアプリケーション「yorisoi-care-app」と、物理ロボットインターフェース「カタノリロボ (katanori)」を連携させるためのアーキテクチャ定義です。
「遠隔からのカメラ監視」といったプライバシーリスクを排除し、**「AIによる自然な対話を通じた情報収集と見守り」**を実現することを目的とします。

## 2. システムアーキテクチャと役割分担

システムは「頭脳（WEB側）」と「口と耳（ロボット側）」に明確に役割を分割します。

*   **頭脳・記憶 (yorisoi-care-app / Cloudflare Durable Objects)**
    *   AI API（Gemini等）との通信、会話のコンテキスト管理。
    *   ユーザー情報・対話ログの蓄積（記憶ジャーナル）。
    *   自発的な質問の生成とロボットへのプッシュ指示。
*   **口と耳 (katanori / ファームウェア)**
    *   マイクからの音声取得（ウェイクワード検知含む）。
    *   WEB側から送られてくる音声データの再生。
    *   UI表現（OLEDによる顔文字表示、基板内蔵LEDの点灯、つなぎ言葉の再生など）。

## 3. 通信仕様と接続フロー

通信はすべてセキュアな **WebSocket (WSS)** を用いて行い、中継・管理は Cloudflare Workers の **Durable Objects (DO)** が担います。音声ストリームはラグ低減のため、16kHz / 16bit PCM等の軽量フォーマットを利用します。

1.  **Wi-Fiプロビジョニング（APモード＋キャプティブポータル）**
    *   ロボットは初回起動時（Wi-Fi未設定時）、自身がアクセスポイント（例: `katanori-setup`）となります。
    *   ユーザーがスマホでそのWi-Fiに接続すると、自動的に設定画面（キャプティブポータル）が開き、自宅のWi-Fiパスワードを入力することでロボットがインターネットに接続されます。
2.  **ペアリングとセキュリティ（なりすまし防止）**
    *   Wi-Fi接続後、ロボットはOLEDディスプレイに自身の識別情報（個体番号）を含んだ**QRコード**を表示します。
    *   ユーザーがWEB側（yorisoi-care-app管理画面）からこのQRコードを読み取ることで、個体番号とユーザーアカウントが安全に紐付きます。
3.  **待ち合わせ・疎通**
    *   ペアリング済みのロボットは起動時、認証トークンと個体番号を用いて、該当するDurable ObjectのWebSocketエンドポイントに接続します。
    *   DOはロボットの接続状態を管理し、WEB側（管理画面）へオンラインステータスを共有します。

## 4. 対話フローと「人格の統一」

どちらが起点となった会話であっても、必ず **DO（yorisoi-care-app）** がAPI（音声in/音声out）を呼び出すことで、**「同じ声・同じ人格・コンテキストの共有」**を担保します。

### A. ユーザー起点（話しかけられた場合）
1.  **エッジAIによるウェイクワード判定（プライバシー保護）**:
    *   ReSpeaker LiteでVAD（人間の声の検知）を行った後、**ESP32内部の軽量な音声認識モデル（TinyML等）**を用いて「カタノリ」と呼ばれたか（ウェイクワード）をローカルで判定します。
    *   クラウドへ日常の音声を送り続けることによるプライバシーリスクと負荷を防ぐため、ウェイクワードが確認された場合のみ、起動音を鳴らしてDOへの通信を開始します。
2.  **音声ストリーミング送信**: ロボットがユーザーの音声（ウェイクワード以降）を録音し、**WebSocketのバイナリストリーム（細かなチャンク）**として順次DOへ送信します（ラグ低減のため）。
3.  DOが受け取った音声ストリームをそのままGemini等のAPI（音声 in / 音声 out）に送信。
4.  APIから返ってきた音声ストリームをDOがロボットへ順次返送し、会話ログを記憶ジャーナルへ保存。
5.  ロボットが送られてきた音声データをスピーカーからストリーミング再生。

### B. yorisoi起点（AIの好奇心・プッシュ）
1.  DO上のAIが記憶ジャーナルをもとに、自発的な質問テキストを生成（例：「足の調子はどう？」）。
2.  **不在確認（呼びかけフェーズ）**: AIの質問を即座にAPIに投げるのではなく、まずはロボット側に事前登録された汎用的な定型音声（「ねえねえ、いまおしゃべりできる？」など）をローカル再生し、ユーザーの反応を待ちます。（※マイコンに保存するため名前等の動的生成は行わない）
3.  **反応ありの場合**: ユーザーが返答した場合のみ、Aのフローに合流してAPIとの本格的な対話（質問テキストからの音声変換とプッシュ送信を含む）を開始します。
4.  **反応なし（不在）の場合**: 一定時間応答がなければタイムアウトとし、AI APIへのリクエストもジャーナルへの記録も行わずに処理を安全に終了します（API無駄打ちの防止）。

## 5. ラグ（遅延）対策とUX向上

クラウドAPIを経由することによる通信・処理ラグ（数秒の沈黙）を隠蔽し、自然な対話テンポを実現するために、ロボット側（マイコン内）で以下のUX処理を行います。

*   **「つなぎ言葉（フィラー）」のローカル再生と排他制御**
    *   ユーザーが喋り終わり、ロボットがDOへ音声データを投げた瞬間に、マイコン内部（内蔵フラッシュメモリ等）に保存された短い音声（「うんうん」「えーっと」「ちょっと待ってね」等）を即座に再生します。
    *   これにより、API処理中の数秒間を持たせ、ユーザーに「待たされている感」を与えません。
    *   **【排他制御】** つなぎ言葉を再生している途中でAPIからの本回答ストリームが届き始めた場合は、即座につなぎ言葉をフェードアウトさせて本回答の再生に切り替えます。

---
**※実装時の次のステップ**
本仕様をもとに、まずは `yorisoi-care-app` 側に「個体番号をキーにしてロボットとWebSocket接続し、相互にメッセージをやり取りできるDurable Objectsの基盤」を構築する作業から開始することを推奨します。

## 6. ハードウェア構成と制約事項
本システムは以下のハードウェア構成を前提として動作します。

1. **メインMCU**: XIAO ESP32S3
   *   SDカード非搭載のため、「つなぎ言葉」等の短い音声データはプログラムメモリ（PROGMEM）または内蔵フラッシュ（LittleFS等）に直接埋め込んで再生します。
   *   基板上の内蔵LEDを補助的なステータス表示（通信中など）に活用します。
2. **音声DSP**: ReSpeaker Lite (XMOS搭載、AEC対応)
   *   ハードウェアAEC（エコーキャンセラー）を活用し、ロボットが喋っている最中でもユーザーからの割り込み発話を高精度に聞き取れるようにします。
   *   **【重要: ルーティング】** AECを正しく機能させるため、オーディオ出力（スピーカー）への音声信号はESP32からI2S経由で一度ReSpeaker Liteへ送り、ReSpeaker LiteのDACからスピーカーへ出力する構成とします。
   *   基板上のステータスLEDを音声認識状態のインジケーターとして活用します。
3. **ディスプレイ**: 0.96インチ SSD1306 OLED
   *   通信ラグを視覚的に隠蔽するため、API通信中はOLEDに「考え中の顔（ローディング表現）」を描画します。
4. **スピーカー**: 8Ω 2W キャビティ付きパッシブスピーカー
5. **電源**: ReSpeaker Lite USB-C (5V給電)
