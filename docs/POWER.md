# 電源設計メモ

肩に載せて持ち歩くための電源をどうするか。**この文書は「いまの設計と、その根拠」だけを持つ。**

| 文書 | 担当 |
|---|---|
| **POWER.md（ここ）** | 回路・定数・その根拠・部品の向き・データシート・却下した案 |
| **[relay_board.html](../hardware/parts/relay_board.html)** | 🔒 **どの穴に挿すか・配線の経路・組む順番。穴の割り当てはこちらが唯一の正** |
| **[TODO.md](TODO.md)** | 何が残っているか |

⚠ **穴番号をこの文書に書かないこと。** 両方に書くと必ず片方が古くなる（2026-08-07 に実際に起きた）。

📄 **経緯・トラブルの捜査記録・退役したユニバーサル基板の穴割りは、2026-08-15 にこの文書から
削除した。** 必要なら `git log -p docs/POWER.md` で読める。**ここに書き戻さないこと。**

### 目次

1. **いまの構成** — PowerBoost 1000C・電池・そのピン配置・LBO の受け方
2. **電源の入切** — リードスイッチで EN を落とす／AS5600 が数度手前でミュートする
3. **ミュートリレー** — 耳を守る仕組み。回路・定数・部品の向き・実測
4. **ハブ基板** — 板の外に出る10口・レイアウトの規則・配線の仕様
5. **5V の受け口** — PowerBoost から ReSpeaker まで 5V が通る1本道
6. **消費電流・測定・INA226** — 見積もり・稼働時間・電流の測り方
7. **却下した案** — 蒸し返さないこと

⚠ **記号は他の文書と同じ。** ✅ 実測 ／ 📄 カタログ ／ ⬜ 未取得 の格付けは
[DIMENSIONS.md](DIMENSIONS.md) の表に従う。🔒 は決めたこと、🔴 は踏むと事故になること。

### ⚠ 用語 — 電源の略称（初出で開く）

| 略称 | 開くと | 何のことか |
|---|---|---|
| **`EN`** | **Enable** | 昇圧のオン/オフ端子。**GND に落とすと 5V 出力が止まる。** 基板側でプルアップ済みなので、何も繋がなければ常にオン。**リードスイッチとマスタートグルはここにぶら下がる**（2章） |
| `LBO` | Low Battery Output | 電池が減ると出る信号。稼働時間はこれが落ちるまでを測る（6章） |
| `VBUS` | — | USB から来る 5V（充電の入り口） |
| `VLIPO` / `VBAT` | — | 電池そのものの電圧（約3.0〜4.2V） |
| `5Vo` | — | 昇圧後の 5V 出力。ReSpeaker へ入れる側（5章） |
| **インダクタ** | — | 🔒 **昇圧のコイル（L1・6.8µH）はこの資料では必ず「インダクタ」と呼ぶ。** 「コイル」はリレーの巻線だけを指す（2026-09-12 ユーザー「揺れないで欲しい」） |
| `COM` / `NO` / `NC` | Common / Normally Open / Normally Closed | リレーと会話ボタンの接点。**NO＝ふだん開いている**（4.5章） |

⚠ **電源以外の略称はここに書かない。**`AGC`・`MD/ML/MH` は [KNOB-ENCODER.md](KNOB-ENCODER.md)、
`NVS`・`OTA`・`AEC` はファーム側の担当。

### ⚠ 用語 — 「板」が3枚あるので必ず区別する

**2026-08-15 まで、この文書は下の①と②をどちらも「旧ボード」と呼んでいた。** そのせいで
「20分の電池駆動」と「リレーの実測」がどちらの板の実績なのかが混ざった。**以後こう呼ぶ。**

| 呼び方 | 何 | いま |
|---|---|---|
| ① **死んだ ReSpeaker Lite** | I2Cバス死＋発熱で退役した1枚目 | 🔴 **電池駆動20分（8/01）はこの板の実績。現行機には引き継がれない** |
| ② **試作のユニバーサル基板** | リレーの駆動回路を最初に組んだ板（8/07） | ✅ **ミュートリレーの実測（8/08）はこの板＋現行 ReSpeaker Lite。** ハブ基板へ組み直す |
| ③ **ハブ基板** | DAISEN PU52X74（4章） | ✅ **2026-08-18 実装完了・導通確認済み**（[TODO.md](TODO.md) A-1）。⚠ 通電はまだ |

⬜ **①を壊した原因は、鉄定規の上で通電してショートさせたことだと思われる**
（2026-09-03・ユーザーの申告。⚠ 現物での確認はしていない）。この筋は症状 4 つを 1 つの機構で
説明する。**発熱**はレール間の短絡、**I2C バス死**は 5V が 3.3V の SDA/SCL に乗って I2C の
ドライバだけが飛んだこと、**ESP32 のブートループと USB 消滅**は電源・IO の実損傷、そして
**XMOS と I2S とマイクが正常**なのは、定規が触れた範囲だけが壊れたからである。
素の状態（外付け配線なし）で再通電しても発熱したという 2026-08-05 の試験結果とも合う
（配線側が犯人なら、外せば止まるはずだった）。

🔴 **これは設計の問題ではなく作業環境の問題なので、同じ机で同じことをすれば再発する。**
ユニバーサル基板の裏は剥き出しのはんだ付けが並んだ面で、金属を当てれば一度に何本もの
ネットが橋渡しされる。**PCB にすると裏はソルダーレジストで覆われ、露出するのはパッドだけ**
になるので、この壊れ方はほぼ無くなる。

🔒 **現行の ReSpeaker Lite（2枚目）は生きている。** 8/05 の「回すとコーデックが死ぬ」の解決も
8/08 のリレー実測も、この板で取っている。

---

## 1. いまの構成

🔴 **実機は組み替えの途中で、「動く構成」そのものがいま存在しない**（2026-08-25・ユーザー）。
最初の電源投入は [FIRST-RUN.md](FIRST-RUN.md) の 1 枚でやる（この文書を読む必要はない）。

⚠ **2026-08-01 の「PowerBoost 1000C ＋ リポ1000mAh で、スピーカーを鳴らしたまま20分連続動作」は
起きた事実だが、それは死んだ ReSpeaker Lite（①）での実績である。**
いま生きている板（2枚目）では**一度も電池で動かしていない。**
🔴 **したがって「1Aで足りるか」は決着していない。**
しかも当時から負荷は増えている（**ミュートリレーのコイル約30mA** は 8/07〜08 に足したもので、
**OLED も 2.42インチに大型化**している）ので、旧ボードの結果をそのまま当てはめることもできない。

⬜ **電池駆動は、ハブ基板を組んだあとに取り直す**（[TODO.md](TODO.md) A-2）。**そこまでは、この
文書の電源に関する記述はすべて設計であって実績ではない。**

| 品目 | 実物 | 備考 |
|---|---|---|
| 充電＋昇圧 | **Adafruit PowerBoost 1000C** | TPS61090昇圧 ＋ MCP73871充電。**ロードシェアリング内蔵** |
| 電池 | **リポ 3.7V 1000mAh** | 容量は暫定。稼働時間の実測を見て決め直す |
| マスタースイッチ | **トグル MTS-102**（6A 125VAC・ON-ON） | EN⇔GND に入れる |

PowerBoost 1000C の出力は連続 1000mA+（内部スイッチ2A・ピーク制限約2.5A）で、**数字の上では
見積もり（下の6章・会話中 約300〜450mA）に対して余裕がある。** 旧ボードで20分持ったことも、
桁が合っていることの傍証にはなる。⚠ **ただし上のとおり実績ではないので、組んだら必ず実測すること。**

### ⬜ 次の常用機 — 充電の口を Type-C にするなら（2026-08-23 調査・未検証）

🔴 **2026-08-25 訂正: 「中で変換ケーブルを挿す」はもう現在の設計ではない。**
第1世代の充電は **Type-C 基板（秋月 115426・左の壁の後ろ下 🔒）で受け、PowerBoost へは USB のパッドに立てたピンへ 2 本で繋ぐ**
（[CASE-V4.md](CASE-V4.md)）。この節は「次の常用機で板ごと選び直すなら」の調査記録としてだけ残す。

**PowerBoost 1000C の Type-C 版は無い。**Adafruit は 2465（micro-B）のままで、
「Type-C 入力・充電・昇圧・ロードシェアリング・EN」を **1枚で満たす板は見つからなかった。**
替えるなら次の2系統になる。

| 案 | 構成 | 4役 | 弱点 |
|---|---|---|---|
| ① 2枚に分ける | Adafruit [bq24074 #4755](https://www.adafruit.com/product/4755) $14.95（USB-C 入力・ロードシェアリング・出力は 4.4V 以下 最大1.5A）＋ [MiniBoost 5V@1A TPS61023 #4654](https://www.adafruit.com/product/4654) $3.95（17.8 × 11.3 × 5.6mm・**EN あり**、Low で出力を完全に切る） | 全部残る | 昇圧が **1A 上限**（現行の TPS61090 は内部スイッチ 2A）。海外発送で早くない |
| ② IP5306 系を1枚 | Type-C 入力・充電・5V/2A 昇圧・パワーパス。Amazon / AliExpress に多数 | ロードシェアリングは有り、**EN が無い** | **出力 50mA 以下で自動オフ**する電源で、EN 相当の端子が無い。⇒ 2章のトグルとリードの切り方を作り直すことになる |

- 🔴 **①に対してユーザーは否定的である**（2026-08-24「2枚に基板を分けるのも得策とは思えない。設計を複雑にするだけ」）。
  まだ②に決まったわけではないが、①を推すなら 1枚に戻せない理由を先に示すこと。
  🔴 **この否定と、下の案③でユーザーが言っている「2 基板に分けておきたい」は食い違って見えるが、別の話である。** ここで否定されたのは既製モジュールを 2 枚買って箱に積むことで、案③のほうは自分で起こした板を V カットで割るかどうかの話である。どちらを採るかは決まっていない。
- 秋月には該当が無い（「充電 昇圧」で 0 件）。TP4056 系の Type-C 充電モジュールはあるが、
  ロードシェアリングが無いので単体では使えない。
- ⬜ **決め手は昇圧の余裕である。**①を採るかどうかは、6章の見積もり（会話中 約300〜450mA）ではなく
  **実測のピーク**で決まる。スピーカーが鳴った瞬間に 1A を超えるなら①は落ちる。
  ⇒ 第1世代で電池駆動の実測を取るとき（[TODO.md](TODO.md) A-2）に、平均だけでなくピークも見ておく。
- ⚠ **この節は Web の商品情報だけで書いてある。現物は1枚も見ていない。**

### ⬜ 案③ 電源の板を自分で起こす（2026-09-03・構想・未検証）

ハブ基板を JLCPCB のような PCBA で作るなら、PowerBoost と INA226 も同じ工程に乗せられる。
この場合、既製の板を探す問題ではなくなるので、上の①②とは別の道になる。

Adafruit は PowerBoost 1000C の回路図と EAGLE の .brd を公開している（[GitHub](https://github.com/adafruit/Adafruit-PowerBoost-1000C)。
[DIMENSIONS.md](DIMENSIONS.md) が寸法の出どころとして既に引いている）。したがって電源回路を
設計する必要はなく、写すだけでよい。中身は **TPS61090（昇圧）＋ MCP73871（充電・ロードシェア）**
の 2 個で、Type-C にするには micro-B のフットプリントを差し替えて CC の 5.1kΩ を 2 本足す。

**この案が成立すると、案①の弱点が 2 つとも消える。**

- 「2枚に基板を分ける」という否定（上の 2026-08-24）は、**既製モジュールを 2 枚買って箱に積む**
  話だった。bq24074 と昇圧 IC をチップとして 1 枚の板に載せるなら、板は 2 枚にならない。
- 「昇圧が 1A 上限」は MiniBoost という**製品**が TPS61023 を積んでいるからで、チップで選ぶなら
  現行と同じ TPS61090（内部スイッチ 2A）を置ける。⬜「決め手は実測のピーク」も、この経路では
  決め手にならなくなる。

JLCPCB はスルーホールも実装する。SMD と THT を 1 回の発注に混ぜられ、THT はフローはんだで、
追加費用は手はんだ工賃 $3.58 ＋ 1 ジョイント $0.0164（2026-09-12 に JLCPCB の料金ページで取り直した。09-03 の時点では $3.5 ＋ $0.0173）、納期は SMT ＋1 日である。
SMT 側は Economic で段取り $8.18・メタルマスク $1.53、Extended の部品は種類ごとに $3.07 かかる（Basic は 0）。この板の口は
33 ピンなので、ヘッダと JST を全部載せて $4 ほどになる。発注に含めなかったスルーホールには
はんだが回らないので、一部だけ後付けにすることもできる。

#### ⬜ 板の分け方（2026-09-03・ユーザーの希望。決定ではない）

ユーザーは「2 基板に分けておきたい。1 枚でもいいがノコギリで切れるようにしたい」と言っている。
分け方は **「自作 PowerBoost ＋ INA226」の電源板**と、**今回作った中継基板（ハブ基板）**の 2 枚である。
1 枚のパネルに V カットを入れて出せば、割るか割らないかを後から選べる。

**リードスイッチとトグルは電源板側である。** どちらもスイッチだが、ネットは `EN` で、
PowerBoost の `EN` を GND に落とすために入っている（電源の口 `S6`＝`EN`、リード `S8`＝`EN`、
トグル `S12`＝`EN`）。したがって `EN` は電源板の中で閉じ、境界には出てこない。
逆に、もう一枚に残る唯一の「回路」はミュートリレーである（リレーの `COM` がスピーカー `IN(+)`、
`NO` が `OUT(+)`、駆動は `D3` → 1kΩ → 2SC1815。3章）。会話ボタンは `GND` と `D2` の 2 本なので、
中継せず XIAO へ直結できる。

境界を渡る線は **5 本**である。

| 線 | 出どころ（いまの口） |
|---|---|
| `5V` / `GND` | 電源の口（`S3` / `S5`。`S4` は空き。`S6` の `EN` は渡らない） |
| `3V3` / `SDA` / `SCL` | 電流計の口（`S17` / `S19` / `S18`。`S16` の GND は電源側と共通化） |

5 本のうち 2 本が I2C なので、**電源板は 5V を送るだけの板ではなく I2C バスの枝になる。**
INA226 のアドレスは実機で確認した `0x44` である（この章の後ろ）。

**電池の線は境界を渡らない。** 電池 → INA226 → PowerBoost が全部電源板の中で完結するので、
[CASE-V4.md](CASE-V4.md) の 🔴「電池の線は電池のタブから電流計の INPUT まで 1 本続きで、外れる所が
どこにもありませんでした」は、線ではなく銅箔になって消える。

V カットは回転刃が板の端から端まで一直線に走る加工なので、**2 枚は一本の直線で接する長方形**で
なければならない。L 字に食い込ませる分け方はできず、それをやるならルーターとミシン目になる。
切り離しを選べるようにするなら、カット線をまたぐ位置に 6 ピンのコネクタ跡を両側に置いておく。
1 枚のまま使うなら未実装でパターンがそのまま渡り、割るなら両側に実装してジャンパで繋ぐ。

⚠ **この案も現物は見ていない。**

#### チップの在庫（2026-09-12 に JLCPCB の部品ページで確かめた）

4 つとも JLCPCB に在庫があり、Economic の実装に使える。**区分は 4 つとも Extended** で、Basic のものは無い。
Extended は部品の種類ごとに 1 回の発注で $3.07 かかる。昇圧・充電・電流計の 3 種類なら、発注 1 回あたり $9.21 である。

| チップ | 役目 | JLCPCB 部品番号 | パッケージ | 区分 | 在庫 | 単価 |
|---|---|---|---|---|---|---|
| TPS61090RSAR | 昇圧 | C206167 | QFN-16（4×4） | Extended | 2,480 | $2.93 |
| MCP73871-2CCI/ML | 充電（ロードシェア） | C5121473 | QFN-20（4×4） | Extended | 293 | $4.41 |
| BQ24074RGTR | 充電（MCP73871 の代わり） | C54313 | QFN-16（3×3） | Extended | 736 | $2.12 |
| INA226AIDGSR | 電流計 | C49851 | VSSOP-10 | Extended | 26,334 | $0.77 |

確かめたのはこの 4 つだけである。インダクタ・USB-C の受け口・JST・抵抗やコンデンサの区分は見ていない。

#### V カットの規則（2026-09-12 に JLCPCB の製造能力のページで確かめた）

- 銅箔は V カットの線から 0.4mm 以上離す。
- 割った縁の寸法の誤差は ±0.4mm で、板の厚さは 0.6mm 以上。
- パネルは最小 70 × 70mm、最大 475 × 475mm。
- 2 本の V カットの間は最小 2mm（推奨 3mm）。

ハブ基板は 74 × 52mm（`hub_board_parts.scad`）である。電源板を長い辺に沿って並べると、パネルは 74 ×（52 ＋ 電源板の幅）になり、70 × 70 を満たすには電源板の幅が 18mm 以上要る。

#### 回路図（2026-09-12・v0.1）

回路図は [hardware/pcb/hub_power/](../hardware/pcb/hub_power/) にあり、`hardware/pcb/gen_sch.py` が生成する。
新しく設計した回路は無く、3 つの出どころを 1 枚に写しただけである。電源は Adafruit PowerBoost 1000C Rev B の
回路図（[hardware/ref/powerboost_1000c/](../hardware/ref/powerboost_1000c/)・CC BY-SA 3.0）、ハブは
[relay_board.html](../hardware/parts/relay_board.html) の部品と `hub_ports.py` の口、電流計は今の INA226 モジュールの役目である。

`hardware/pcb/check_sch.py` が、KiCad の書き出したつながりを写した元とピン 1 本ずつ突き合わせる。
v0.1 では合わない所は 0 件だった。わざと 5 か所を壊すと 5 か所とも検出したので、空振りではない。

写したときに変えた点は次のとおりである。

- PowerBoost の USB-A の出口・その D+/D- の分圧抵抗 4 本・端子台・8 本のヘッダは写していない。5V は板の外へ出さず、板の中でハブの +5V に渡るからである。
- 電池の + と充電 IC の VBAT（PowerBoost の `VLIPO`）のあいだに、INA226 のシャント 10mΩ を入れた。アドレスは A1 を 3V3、A0 を GND にして今と同じ `0x44` にした。
- 充電の口は **USB-C の受け口（J13・`C165948`・TYPE-C-31-M-12）を電源板に載せた**（🔒 2026-09-12 ユーザー）。
  CC の 5.1kΩ × 2（R44・R45）も板の上に載る。⇒ 秋月の Type-C 基板（115426・¥180）と、そこからの線 2 本と、
  手で付ける CC 抵抗が無くなる。JLCPCB はこの受け口を SMT の流れで付ける（部品ページの Assembly Type）。
  ⚠ **板の縁を壁へ持っていくのは筐体 v6 側の仕事**で、充電の口の位置が左の壁の高い所へ移る。
- 電池の JST-PH（J10）は PowerBoost と同じく 1 番が −、2 番が + である。🔴 板が届いて最初に電池を挿す前に、J10 の 2 番と GND のあいだが電池の + と − の向きに合っているかをテスタで確かめる（2026-09-11 の逆接の件）。
- ハブの口 8 つ（XIAO・OLED・AS5600・スピーカー IN/OUT・リード・トグル・会話ボタン）は、`hub_ports.py` のピンの並びのまま番号を振った。
  🔒 **ただし J2（OLED）だけ並びを逆にした**（2026-09-12 ユーザー）。OLED を **HS154L03W2C01**
  （1.54"・128×64・SSD1309・I2C・`C7465999`・$6.94・在庫 1,242）に替える前提で、パネルの
  **1 GND・2 VCC・3 SCL・4 SDA**（📄 データシート 1.5 Pin Definition）に合わせた。穴の位置は動かしていない。
  ⚠ **いまの OLED の線はこの板に挿せない。**戻すなら `gen_sch.py` の `PORT_FLIP` を空にする。
  ⚠ `check_sch.py` は `PORT_FLIP` を読んで同じ並びを期待するので、**この 1 か所は機械で検算していない**。
  正はデータシートの図（PCB 42.40 × 38.00 × 2.65・取付穴 φ3.0 × 4・中心間 37.40 × 33.00・端子 2.54 の 4 本）。電源と電流計の口は板の中の配線に変わって無くなる。
- I2C の引き上げ 10kΩ を 2 本載せた（R42・R43）。今の電流計モジュールに引き上げが載っているかは確かめていない。

KiCad の電気的なチェック（ERC）で残るのは、Q1（MMUN2133）のベースを駆動する出力が無いという 1 件だけである。
MMUN2133 はベースとエミッタのあいだに抵抗を内蔵しているので、実物では浮かない。

#### レイアウトと配線（2026-09-12・v0.1）

パネルは **74 × 86mm** で、上が**ハブ 74 × 52**、下が**電源 40 × 32**、あいだ 2mm をミシン目のタブ 2 本でつなぐ。
1 枚で発注して、使うときに割る。電源板の大きさは、いまの PowerBoost の場所（天井）で当たりを見て決めた
（`hardware/_tmp_pcb/pbfit.scad` で `one`/`others` と当たりを取り、後ろ＝ハッチ側へ伸ばす向きなら 32mm まで当たり 0。
前へ伸ばすと 5mm でスピーカーのバスタブの腕に当たる）。

ハブ側の口・リレー・会話ボタン・取付穴は、いまのユニバーサル基板と同じ座標に置いた（`hub_ports.py` と
`relay_board.html` から機械で読む）。**筐体もハーネスもそのまま使える。**

🔴 **板をまたぐ口を足した。** J11（ハブ側）↔ J12（電源側）の 7 本で、並びは 5V・空き・GND・3V3・SDA・SCL・EN。
渡るネットが 6 本になるのは、リードとトグルの口がハブ側にあるからである（この章の上の「5 本」はそれらを
電源板側に付ける前提で書かれている）。**板ごとにネット名を分けてある**（電源板側は V5P・GNDP・V33P・SDAP・SCLP・ENP）。
2 枚は銅箔ではつながらず、つながるのは J11 ↔ J12 のハーネスだけだからである。

配線は Freerouting（`hardware/_tmp_pcb/freerouting/`・インストールせず展開しただけ）で引いた。
結果は **未配線 0・違反 0**、KiCad の DRC も**エラー 0**である（残る警告はシルクの文字の重なりと、
足形がライブラリと少し違うという指摘だけ）。線幅 0.2mm・間隔 0.15mm・ビア 0.8/0.4mm。

生成と検査は全部スクリプトで回る。

| ファイル | 役目 |
|---|---|
| `hardware/pcb/gen_sch.py` | 回路図を作る |
| `hardware/pcb/check_sch.py` | 回路図を写した元（PowerBoost・relay_board・hub_ports）と突き合わせる |
| `hardware/pcb/gen_pcb.py` | 部品を置いた基板と、自動配線へ渡す DSN を作る |
| `hardware/pcb/dsn.py` | DSN の書き出しと SES の読み込み（KiCad のコマンド版には無い） |
| `hardware/pcb/route.py` | 自動配線を回して結果を基板へ入れ、DRC をかける |
| `hardware/pcb/check_pcb.py` | 銅箔どうしの最小すきまを **KiCad とは別の道で**測る（同じ道具で 2 回測っても独立した観測にならないため） |

#### 部品と製造ファイル（2026-09-12）

`hardware/pcb/fab.py` が、ガーバー・穴・部品表・実装位置を `hardware/pcb/hub_power/fab/` に書き出す。
発注に上げるのは `hub_power_gerber.zip`（ガーバーと穴だけ）と `fab/bom.csv`・`fab/cpl.csv` である。

製造ファイルは **表面実装の 44 個（28 種類）**を頼む形で、**足付きの部品 11 個**
（ピンヘッダ 8 個・電池の JST・スピーカーの PH 2 個）は部品表と実装位置から外れている。

🔒 **足付きの 11 個は自分で付ける**（2026-09-12 ユーザー「自分でつけるよ。それくらい簡単だしね」）。
⚠ **穴は JLCPCB が開けてメッキまでして届く。**板の製造に含まれていて追加料金は無い。
自分でやるのは、開いている穴に部品を挿して裏から半田付けするところだけで、2.54mm なので
表面実装のような細いこて先は要らない。

頼む選択肢もあり、その場合は **はんだ点 41 点で、手はんだ工賃 $3.58 ＋ 41 × $0.0164 ＝
発注 1 回あたり $4.25**（5 枚でも 100 枚でも 1 回）。いまは頼まない。

🔴 **ミュートリレーのトランジスタとダイオードは表面実装に替えた**（2026-09-12 ユーザー「トランジスタと
ダイオードは付けてよ」）。**2SC1815-GR → SS8050（SOT-23・`C2150`・Basic）**、**1N4148 → 1N4148W
（SOD-123・`C81598`・Basic）**。どちらも追加料金のかからない Basic で、SMT の流れで付く。
SS8050 は hFE 200〜350（2SC1815-GR は 200〜400）・Ic 1.5A なので、コイルの 30mA では 2SC1815 と同じく
深く飽和する。ベースの 1kΩ、🔴 安全の要である 10kΩ のプルダウン、COM–NO の使い方は変えていない。
⚠ 足付きのままにすると、JLCPCB では手はんだの工賃（$3.58 ＋ 1 点 $0.0164）がかかるうえ、
2SC1815 と DO-35 の 1N4148 はどちらも Extended（種類ごとに $3.07）だった。

🔒 **ミュートリレーも表面実装に替えた**（2026-09-12 ユーザー「リレーに準じる物であればなんでもいい。
拘りはない」）。**Y14H-1C-5DS → Omron G6S-2F DC5（`C16707`・Extended・$1.47・在庫 4,310）**。
スルーホールだったのは手元の実物がそうだったからで、技術的な理由は無かった。替えたことで
①機械が載せるので手はんだが消え、②足形が KiCad 標準の `Relay_SMD:Relay_DPDT_Omron_G6S-2F`
（Omron が引いた land pattern）になって、私が実物の穴から起こした 6 穴の自作足形は要らなくなった。
コイルは 167Ω 30mA → **178Ω 28mA**（140mW）、接点は 2A@30VDC、非ラッチング（単安定）。
ピン番号は Omron のデータシート（en-g6s.pdf 5 ページ・Top View）と KiCad の記号の**両方**で確かめた:
**1 = コイル ＋／12 = コイル −／4 = COM・3 = NC・5 = NO**（もう 1 回路は 9 = COM・10 = NC・8 = NO）。
**使うのは 1 回路だけ**で、もう 1 回路は未接続にしてある（今の回路をそのまま写した形）。
本体は今の基板でリレーが座っていた 6 穴の真ん中に置いた。

🔴 **JLCPCB にはお金のかからないリレーが 1 つも無い**（2026-09-12 に確かめた）。リレーの棚 30,930 件に
Basic の絞り込みをかけると残り **0 件**、Promotional Extended でも **0 件**で、全部 Extended である。
対照として同じ操作を抵抗でやると Basic は 185 件残るので、絞り込みは効いている。
**つまりリレーを機械で載せるなら、どれを選んでも $3.07 は避けられない。**手で付けるなら 0 円（手元の実物）。

🔒 **Extended 9 種類のまま行く**（2026-09-12 ユーザー「OK Extend でやろ」）。1 回の発注につき $27.63。

区分は **Basic が 19 種類・Extended が 9 種類**である（2026-09-12 に JLCPCB の部品検索で 1 つずつ
確かめた）。Extended は種類ごとに $3.07 なので、**発注 1 回あたり $27.63**（5 枚を 6 回まわすなら総額 $166）かかる。Extended として残っているのは、
昇圧 `C206167`・充電 `C5121473`・電流計 `C49851`・MMUN2133 `C21714196`・インダクタ `C167807`・
シャント `C105362`・1.87MΩ `C482988`・USB-C の受け口 `C165948`・リレー `C16707` の 9 つで、
どれも代わりが利かないか、代えると回路の意味が変わるものである。

#### Extended を 12 → 9 種類に減らした（2026-09-12・🔒 ユーザー決定・実施済み）

PowerBoost から写した定数のうち 3 つは、**回路の意味をほとんど変えずに Basic の値へ寄せられた**。
1 種類 $3.07 なので、3 つ落として**発注 1 回あたり $9.21**（5 枚を 6 回まわすなら総額 $55）減り、Extended は 9 種類・発注 1 回あたり $27.63 になった。
C6 は下の (b)（47µF 10V 1206 を 2 個）を採った（ユーザー「b でいいでしょ」）。
下の区分は、JLCPCB の部品検索で値ごとに Basic の絞り込みをかけて残り件数を読んだものである
（対照として抵抗全体に同じ操作をすると 185 件残るので、絞り込みは効いている）。

| 元の部品 | 役目 | 替えた先 |
|---|---|---|
| 340kΩ（R2・`C17636`・Extended） | 電池切れの知らせ（LBO）の閾値を決める分圧の下側 | **330kΩ 0603 `C23137`**（Basic・在庫 103 万） |
| 270kΩ（R6・`C17589`・Extended） | USB が来ているかを見る分圧の上側 | **220kΩ 0603 `C22961`**（Basic・在庫 160 万） |
| 100kΩ（R7・Basic） | 同じ分圧の下側。R6 と比を保つために一緒に動かした | **82kΩ 0603 `C23254`**（Basic・在庫 143 万） |
| 100µF 10V 1210（C6・`C23742`・Extended・$0.45） | 昇圧した 5V の平滑 | **47µF 10V 1206 `C96123`（Basic）を C6 と C9 の 2 個**（$0.27 × 2） |

**① 340kΩ → 330kΩ。** R1 1.87MΩ と組んだ分圧の下側で、TPS61090 の LBI が 0.5V を割ると
LBO が出る。閾値は `0.5V × (1 + R1/R2)` なので、340kΩ なら 3.25V、330kΩ なら **3.33V** になる。
**80mV だけ早く「電池が減った」と言うようになる**。電池を使い切る側ではなく余らせる側へ動くので、
稼働時間がわずかに短く出るほかに実害は無い。

**② 270kΩ → 220kΩ（同時に R7 100kΩ → 82kΩ）。** VBUS を分圧して MCP73871 の VPCC へ入れ、
USB の電圧が足りているかを見ている。効くのは比だけで、100/370 = 0.2703 が 82/302 = 0.2715 になる。
**差は 0.45% で、しきい値はほぼ動かない。** 100kΩ は R17 が使い続けるので種類は減らないが、
82kΩ は Basic なので種類が 1 つ増えても料金はかからない。

**③ C6 の 100µF をどうするか。** ここだけは代償がある。この部品は昇圧した 5V にぶら下がる
平滑の大物で、常に 5.2V が掛かっている。**セラミックコンデンサは掛かっている電圧が高いほど
容量が落ちる**性質があり、落ち方は耐圧とケースの大きさで決まる。いま使っているのは 10V 品の
1210 で、Basic に降りるには次のどちらかを選ぶことになる。

| 案 | 部品 | 値段 | 耐圧 | 大きさ |
|---|---|---|---|---|
| (a) 同じ値に替える | `C15008` 100µF 6.3V X5R 1206 | $0.15 | **10V → 6.3V** | 1210 → 1206 |
| **(b) 2 個に分ける（採用）** | `C96123` 47µF 10V X5R 1206 × 2 | $0.55 | 10V のまま | 1206 が 2 つ |

(a) は耐圧とケースの両方が下がるので、5.2V が掛かった状態で実際に残る容量は今より確実に減る。
**どれだけ減るかはメーカーの曲線を見ていないので分からない。** (b) は耐圧が今と同じなので、
この分からない部分を踏まずに済む。**この理由で (b) を採った。**
足した 1 個は `C9` という名前で、電源板の C6 の隣（板の左上を原点に x 36.5・y 13.0）に置いた。
⚠ **C9 は PowerBoost に無い部品で、私が足したものである。**

#### 残る 9 種類はどこまで詰められるか（2026-09-12）

**3 つは、替えるのがもったいない。**昇圧 TPS61090・充電 MCP73871・電流計 INA226 を代えると、
PowerBoost 1000C の写しではなくなる。🔒 **写しであること自体が、この設計の唯一の裏付けである**
（2026-09-12 ユーザー「PowerBoost でさんざん立証されてる安全な回路だから」）。公開された実証済みの
回路をそのまま使い、元の EAGLE の回路図と 1 ピンずつ機械で突き合わせて検証できる
（`check_sch.py`）。**まだ実物が 1 枚も無い今、手元にある確からしさはこれだけである。**
発注 1 回あたり $3.07・6 回まわしても総額 $18 と引き換えに差し出すものではない。

USB-C の受け口も Extended だが、これは棚に Basic が無い（下）。足形を自分で起こしてある件は
**私の手間の話であって、判断の理由にはならない**（2026-09-12 ユーザー「そりゃキミの手間だろ」）。

**リレーは決着済み**で、Basic にも Promotional Extended にも 1 件も無い（上の 🔴）。

**1.87MΩ は E96 なので Basic に無い。**2MΩ 0603 なら Basic に 1 件ある（確認済み）。ただしこの抵抗は
2 か所で使っていて、R3 は出力電圧を `0.5V × (1 + R3/R4)` で決め、R1 は LBO の閾値を決める。
2MΩ にすると出力が 5.5V、LBO が 3.53V になるので、**R4 と R2 も一緒に振り直さないと成立しない**。
値段 $3.07 に対して 5V そのものが動くので、割に合わない。

**インダクタ（L1）とシャント（R41）も、Basic には替えられない**（2026-09-12 に確かめた）。

**インダクタ 6.8µH は Basic に 0 件。**10µH で引くと Basic が 2 件あるので、絞り込み自体は効いている
（対照が取れている）。ただしその 2 件は `C1035`（0603・多層・**3mA**・1.85Ω）と
`C1046`（0805・多層・**15mA**・1.15Ω）で、どちらも信号用の小さな部品である。昇圧は 2A 級を流すので
**桁が 2 つ足りず、使えない**。Basic の棚にある「Inductors, Coils, Chokes」は信号用だけで、
電源用のインダクタは 1 つも無い。

**シャント 10mΩ も Basic に 0 件。**こちらは「Current Sense Resistors」という区分ごと Basic に
1 件も無い（15,376 件中 0）。いま使っているのは `FMF06FTHR010-LH`（`C105362`・10mΩ 1W ±1% 1206・
$0.0425・Extended）である。値で 10mΩ を引いても Basic は 0 件なので、ふつうの抵抗で代用する道も無い。

⚠ 以前ここに「同じやり方で PNP を引くと 0 件になるので方法が信用できない」と書いたが、
**PNP が 0 件だったのは方法の不具合ではなかった**。MMBT3906 を品番で引くと Extended で、
Basic の棚に PNP のトランジスタが無いこと自体が事実だった（2026-09-12 に確かめ直した）。

🔴 **LED の色を 2 つ替えた（私の判断・2026-09-12）。** PowerBoost は電源の印が青、充電中が橙だが、
JLCPCB の Basic に 0805 の青と橙が無く、Extended のものは在庫 0 だった。**青 → 白 `C34499`、
橙 → 黄 `C2296`** にしてある。元の色に戻すなら、種類ごとに $3.07 と在庫待ちが要る。

インダクタ（L1）は PowerBoost の TDK VLC5045（5×5mm）ではなく、在庫のある **FNR4018S6R8MT（4×4mm・6.8µH・2A）**
にした。実機の電流は待機 300〜325mA・充電中 505〜560mA（この章の後ろ）なので、2A で足りる。

#### 発送は LCSC・JLCPCB・JLC3DP を 1 つにまとめられる（2026-09-12 に確かめた）

**LCSC の部品・JLCPCB の基板と実装・JLC3DP の 3D プリントを、1 つの箱にまとめて送らせられる。**
📄 [LCSC の FAQ](https://www.lcsc.com/faqs/notice/combine-lcsc-jlcpcb-orders)・
[JLCPCB Combine Shipping Service](https://jlcpcb.com/help/article/combine-shipping-service)。

やり方は 2 つで、**LCSC の支払い画面で JLCPCB の注文を選んで統合する**のがいちばん簡単。
既に注文済みなら LCSC のサポート（support@lcsc.com）へ両方の注文番号を送って頼む。

| | 内容 |
|---|---|
| 対象 | PCB・SMT・3D・CNC・メタルマスク（FAQ の原文） |
| 条件 | 同じ通貨・同じ顧客 ID。中国本土宛は不可（日本は可） |
| 送料 | 統合後に**再計算**される。不足があればサポートから連絡が来る |
| ⚠ 期限 | **LCSC 側が発送済みだと統合できない。**先に部品だけ注文して放置すると間に合わない |
| ⚠ 取り消し | 一度統合すると**分割も解除もできない** |

🔴 **これは OLED を LCSC で買う前提（下）で効く。**JLCPCB で買った部品は
「今後の PCBA 発注にのみ使われ、単体では発送できない」ので、筐体に付けるパネルは LCSC で買う。
同じ品番 `C7465999` が LCSC にあり、1 枚 $6.57（10 枚以上で $5.64）・在庫 1,242。

**ざっとの費用（5 枚を 1 回で発注したときの総額・板代と送料を除く）**: 段取り $8.18 ＋ メタルマスク $1.53 ＋ Extended $27.63
＋ 部品代 1 枚あたり約 $10（5 枚で約 $50）＋ はんだ点 約 575 点で $0.92 ＝ **約 $88**。
（リレーを表面実装にして $10 ほど上がり、Extended を 3 種類減らして $9 ほど下がった）
⚠ 部品には最低数量があるので上ぶれする。板代と送料と、違う設計を 1 枚に混ぜる追加料金は、
発注画面にファイルを上げるまで分からない。

⬜ **まだ決まっていないこと**（2026-09-12 時点）

- コネクタの向き。電池の JST（J10）とスピーカーの PH（J4・J5）は、線がどちらへ抜けるかで
  向きが決まる。いまは真上から挿す形で置いてある。🔒 **これは筐体（v6）を引いてから決める**
  （2026-09-12 ユーザー「筐体設計によって変わるので今決める事じゃない」）。
- 発注そのもの（板の枚数・送料・違う設計を混ぜる追加料金）。

#### 🆕 AS5600 を中継基板に載せる（2026-09-13・位置と設計はあとで）

つまみの **AS5600 を、口で受ける外付けモジュールではなく、中継基板（ハブ基板）の上のチップにする**
話が出た（2026-09-13 ユーザー）。🔒 **位置と設計はいま決めない**（同・ユーザー「位置や設計は一旦あとで」）。

いまの v0.1 では AS5600 は板の外にいて、`hub_ports.py` の 8 つの口のうち **J3** で受けている
（`gen_sch.py` の `PORT_REF`）。板に載せれば、この口と、[KNOB-ENCODER.md](KNOB-ENCODER.md) 配線章の
**5 本のハーネス**（`VCC` `GND` `DIR` `SCL` `SDA`）が無くなる。

⚠ **決めるときに一緒に決まるもの**（いま触らない）:

- **[KNOB-ENCODER.md](KNOB-ENCODER.md) の 🔒「AS5600 の基板は穴に合う棒＋E リングで留める」（2026-08-28 決着）と
  競合する。** あの決定は外付けモジュールを留める話なので、チップが板に載るなら前提が消える。
  🔴 **どちらも消していない。** 決めるときに片方を落とすこと。
- 板の外形。この章の 74 × 52 は手配線のユニバーサル基板の穴の座標を写した履歴なので、
  外注に切り替えるなら v6 の筐体に合わせて引き直せる（2026-09-12 ユーザー「サイズは筐体に応じて
  変えられる。配線が無くなるから相当自由」）。

⚠ 次の 1 行は AI（私）が足した読みで、ユーザーの発言ではない: AS5600 は磁石を**真上**で読むので、
板に載せるとつまみの軸の位置が板の上の 1 点に固定される。

#### 🆕 v6 の筐体から来た条件と、板の面積・背の高さ（2026-09-13）

筐体 v6（縦置き）を起こしている側から、板に課される条件が来た。ここは**その条件に対して
回路側が何を出せるかの記録**で、決定ではない。決めるのはユーザー。

**筐体側の条件**（2026-09-13・筐体セッション）

- 板は 1 枚（電源基板を中継基板へ統合）。🔒 ユーザー 2026-09-13。J11・J12 とミシン目のタブは消える
- 外形 **44 × 81.2 = 3573mm²**（いまの 2 枚の合計 74×52 ＋ 40×32 = 5128mm² の 70%）
- 部品は前面（+Z）が基本。裏は背面の壁まで 1.0mm しかない
- AS5600 はチップ（`C79815`・SOIC-8）で板に載せる・マスタートグルは板に実装するスライドスイッチへ

**天井は板の面から、区画ごとに違う**（2026-09-13 の訂正版。座標は**板の左下が原点**）。
最初に来た「前面は一律 4.0mm まで」は電池の影の下だけの値だった。

| 区画 | 板の座標 | 面積 | 天井 | 上にある物 |
|---|---|---|---|---|
| **電池の影** | X 6.5〜36.5・Y 24.2〜64.2 | 1200mm² | **4.8** | 電池の底 |
| Z1 | X 0〜35・Y 0〜25 | 875mm² | **12.4**（谷は 9.0） | ReSpeaker の XIAO 面 |
| Z2 | X 0〜35・Y 75〜81.2 | 217mm² | **12.4** | 同 |
| Z3 | X 35〜44・Y 0〜81.2 | 731mm² | **14.2〜17.2** | 歯車・OLED の裏・前の壁 |
| 🔴 立入禁止 | X 33.2〜39.2・Y 4.0〜10.0 | — | — | 磁石と軸の柱（板の面から全高） |

⛔ **電池の影の最初の数字（X 0〜35・Y 25〜75・天井 4.4）は取り下げる。**
🔒 2026-09-13 夜、筐体側が電池（603040・800mAh）の位置を確定した。
**X 6.5〜36.5・Y 24.2〜64.2・底は板の面から 4.8。**

底が 4.4 から 4.8 へ上がったのは、**J2（OLED の PH 4 ピン横型・背 4.8）を X で避けられなかった**ため。
電池の右に残る帯は X 36.5〜44 の **7.50mm** しかなく、PH 4 ピンはいちばん細い向き（90°）でも **8.60**
── **1.10 足りない**。板の全面を 0.5mm 刻みで当てて、電池より上（Y 64.2〜81.2）には **0 通り**、
下（Y 0〜24.2）には 9 通りあるが**全部 J6 が居る谷**、という結果だった
（4 ピンの口は J2 と J6 の 2 つあり、置ける所は 1 つしかない）。
⇒ **X で避けられないので Z で避けた。**電池の底を J2 の天面と面一にした。
代金は**膨らみの余り 1.16 → 0.355**（筐体側の数字）。

⭐ **控えの DTP502535（幅 📄 Max 25.5）へ回ることになったら、J2 は電池の外に出る。**
底を 1.8 に下ろせて、膨らみの余りは 2.2 以上に戻る。
⚠ 容量が半分になるので、**これを理由に替える話ではない**（控えへ回ったときの良い面としての記録）。

##### 電池の帯を決めているのは J2 の高さ — 横挿し 4 ピンの背比べ（2026-09-13）

筐体側が「電池が入る帯は **5.355**（床 ＝ J2 の天面 9.400 ／ 天井 ＝ ReSpeaker の U3 の底 14.755）で、
**厚み 6.0 の電池は 0.645 入らない**」と出した。**電池の下に居る口は J2 ただ 1 つ**なので、
🔴 **0.645 を作っているのは J2 の高さ 4.8 である。**

⇒ 調停役の求めで、**背の低い横挿しの 4 ピン**を 📄 で並べた。
**ピッチではなく「挿した高さ」で読むこと。**

| | 型番（4 ピン・横挿し） | 挿した高さ | 奥行き（挿した状態） | ピッチ | 適合電線 | 付け方 | 床 | **帯** | 6.0 |
|---|---|---|---|---|---|---|---|---|---|
| **PH（いま）** | S4B-PH-K-S | **4.80** | 11.2 | 2.0 | #32〜24 | スルーホール | 9.40 | **5.355** | 🔴 0.645 足りない |
| GH | SM04B-GHS-TB | 4.35 | 7.15 | 1.25 | #30〜26 | SMD | 8.95 | 5.805 | 🔴 0.195 足りない |
| **ZH** | S4B-ZR-SM4A-TF | 3.70 | 7.4 | 1.5 | #32〜26 | SMD | 8.30 | **6.455** | ✅ 0.455 余る |
| **SH** | **SM04B-SRSS-TB** | **2.95** | 6.25 | 1.0 | #32〜28 | SMD | 7.55 | **7.205** | ✅ 1.205 余る |

📄 は `hardware/ref/jst_sh/eSH.pdf`・`hardware/ref/jst_gh/eGH.pdf`・`hardware/ref/jst_zh/eZH.pdf`
（JST の英文カタログ。2026-09-13 に jst-mfg.com から取った）。
⭐ **SH の 2.95 は 2 つの図で裏が取れている** ── 3 ページ Side entry の側面図が **2.9 ＋ 浮き 0.05**、
1 ページの「挿した状態の Connector outline」が **(2.95)**。⇒ 相手は口より上に出ない。
⚠ **J2 を替えても床は 1.8 までは下がらない**（次に高いのが L1 の 1.8）。それでも帯は 8.355。

**代償（2026-09-13・私が調べた範囲）:**

1. 🔴 **手で圧着できない。**SH の接点は SSH-003T-P0.2-H・圧着機 AP-K2N（📄 2 ページ）。
   手持ちの IWS-2520M は対象外。ZH も専用工具。
2. ⭐ **ただし SH 1.0mm の 4 ピンは Qwiic / STEMMA QT の規格そのもの**で、既成ケーブルが多い。
   ⚠ 並びが **GND・VDD・SDA・SCL** に固定で、いまの J2 は **GND・VCC・SCL・SDA**（SDA と SCL が逆）。
   基板側で入れ替えれば既成ケーブルが挿さる（`gen_sch.py` の `PORT_FLIP` と同じ 1 行）。
   ⬜ **「SH 4 ピン ⇔ 2.54 オス」のケーブルが手に入るかは未確認。**
3. ⭐ **手はんだの工賃が減る**（得）。PH はスルーホールで手はんだ（$3.58 ＋ 1 点 $0.0164）、
   SH と ZH は SMD で SMT の流れに乗る。⚠ 代わりに手で付け直せない。
4. ✅ 電流は問題ない。OLED は 20mA 見当で AWG#28 の余裕の内側。
5. ⬜ **区分（Basic / Extended）は未確認。**`SM04B-SRSS-TB` は LCSC `C160404`・$0.215・在庫 38,235。

🔒 **決めるのは調停役。**⭐ **いまの PH のままでも、届いた電池が 6.0 でなければ成り立つ。**

##### 🔴 v6 の作り直し — 決まった事・まだ引いていない事（2026-09-13 夜）

⚠ **この節はまだ板に入っていない。**`gen_pcb.py` も `gen_sch.py` も触っていない。
🔒 **調停役の方針: ①〜⑥ が全部そろってから、引き直しは 1 回で。**
（同じ所を何度も引き直させた、とユーザーに言われた日なので）

**きっかけ**: ユーザー「**これは板ですか、箱ですか？**」。
厚みを 23.00 → 26.29 に増やす案（厚み ÷ 幅 0.479 → 0.548）を出したところで止まった。
⇒ **積み重ねを与件として受け取ったまま隙間の配分だけ裁定していた**（調停役の自己申告）。

**見つかった道**: 中継基板は ReSpeaker の面から **3.90**（はんだ 0.2 ＋ XIAO 1.2 ＋ 樹脂 2.5）に置ける。
いま 12.4 も空けているのは、**中継基板の部品が ReSpeaker の側を向いて生えているから**。
⇒ **背の高い物だけ裏面（背面を向く側）へ移せば、厚みを増やさずに済む**（調停役の机上で 22.10）。

| 隙間 3.90 を超えている物 | 背 | 超過 | 枠 |
|---|---|---|---|
| **K31**（リレー） | 9.33 | +5.43 | 163.7mm² |
| J1・J14（XIAO の口） | 8.50 | +4.60 | 133.0mm² ⇒ **直付けにすれば部品ごと消える** |
| **J2・J4・J5・J6・J10**（口 5 つ） | 4.80 | +0.90 | 365.3mm² |
| **SW1** | 4.00 | **+0.10** | 96.5mm² ⇒ ⬜ **真上に ReSpeaker が無ければ表に残れる** |

⇒ 🔒 **裏へ回すのは 6〜7 個・544mm²（板の 15%）。表に 45 個が残る。**
⭐ **抵抗・コンデンサ・IC・インダクタは全部 1.80 以下**で 3.90 に収まるので、**電源ブロックは動かさない。**

**🔒 決まった条件（2026-09-13・調停役が承認）**

1. **U4（AS5600）は表に残す。**裏へ回すと磁石との間に板 1.6 が入り、
   **エアギャップ 1.10 ±0.41 → 2.70 ±0.41** で 📄 の推奨上限 3.0 に迫る。
2. **XIAO のピンの列の周り 2.5mm を、裏面の立入禁止にする**（箱 X 10.55〜12.25 と 25.78〜27.48・
   Y 5.05〜21.99 の 2 本の帯 ＋ 2.5・約 250mm²・板の 7%）。
   **理由は部品の当たりではなく、14 本を裏から半田付けするこて先の空き。**
   ⚠ `gen_pcb.py` の keepout に入れる。**まだ入れていない。**
3. **口・線・電池を裏の XY にどう並べるかを、引く前に決める。**

**🔒 電池は「閉じた部屋」に入れる**（2026-09-13・調停役が書き直した裁定）

    **電池は「床の上」ではなく「閉じた部屋」に入れる。線もプラグも、その部屋に入ってこない。**
    ⇒ 電池の四方と天地を殻の実体で囲む。開くのは背面の蓋の側だけ。

🔴 **理由**: リポの外装は薄いアルミラミネート。**プラグの角も、切った足も、突き刺さる。**
⚠ **「隣を這う」は「いつか当たる」と同じ意味。**
（この 1 段は、口が裏を向くと線 10 芯がリポの隣を通る、という指摘から出た）

**⬜ まだ決まっていない事**

- ⬜ **電池の四角**（デザインが出す）。これが無いと裏の XY を並べられない
- ⬜ **SW1 の真上に ReSpeaker があるか**（無ければ裏へ回るのは 6 個）
- ⬜ **口を SMD にするか**（⭐ 裏面の実装がどのみち発生するので、費用の上では組にした方が得）
- ⬜ **XIAO を直付けにするか**（g 3.9 ＝ 裏へ 0.5 を推している）

**⚠ 引いてみるまで分からない事**

🔴 **2 層で通るか。**いま配線 732 本が通っているのは **B.Cu が GND のベタでほぼ丸ごと使えるから**で、
裏に 544mm² のパッドが乗ると**ベタが割れる**。⭐ 板の 15% なので**たぶん通る**が、**見込みであって保証ではない。**
⇒ **通らなければ 4 層**（板代が上がる）。**そのときに費用を出す。**

**⚠ 両面実装の費用**: 段取り $8.18 ＋ メタルマスク $1.53 が 2 面ぶんで **＋$9.71 見当**。
🔴 **これは推定。**JLCPCB は「double-sided assembly には追加の段取り費がかかる」としか書いておらず、
**金額を公開していない。**

##### 板の裏へ出る物（仕切りの予算）— 2026-09-13

電池が板と ReSpeaker のあいだから背面の独立した部屋へ移り、**板の裏に仕切りが入る**ことになった。
🔒 **仕切りに足の逃げ穴は彫らない**（彫るといちばん危ない所がいちばん薄くなる。リポの外装は
薄いアルミラミネートで、足の先が当たれば刺さる）。⇒ **逃げは空気で取る。**
⇒ **裏へ出る足の長さが、そのまま Z の予算を食う。**

| | 裏へ | 出どころ |
|---|---|---|
| **J2・J4・J5・J6・J10（JST PH・スルーホール）** | **1.80** | 🔒 📄 `ePH.pdf` 3 ページ (3.4) − 板厚 1.6 |
| **J13（USB-C）** | **0.00** | 🔒 KiCad の STEP（`HRO_TYPE-C-31-M-12.step` の Z −1.040〜+3.160。足 1.04 ＜ 板厚 1.6） |
| **SW1（スライドスイッチ）** | **0.00** | ⚠ **幾何の筋のみ・測定ではない**（表面実装のパッドが板に着く以上、足は板厚以下のはず） |
| **J1・J14（2.54 のメスソケット）** | **1.40**（足 3.0 を買った場合） | ⚠ 足の長さは**部品の性質ではなく買い方の選択**（3.0 / 6.0 / 11.0 が一般に売られている） |
| 表面実装のその他 | **0.00** | — |
| （参考）SMD の口に替えた場合 | **0.00** | 足が無い |
| （参考）XIAO を直付けにした場合 | **0.50** | 6.0 − g − 1.6（g 3.9 を推し。[[はんだの盛りの上限]]） |

⇒ 🔴 **いま裏を決めているのは、口 5 つの 1.80 だけ。**
⚠ **ソケットの 1.4 は 1.80 より小さいので、いまは効いていない。**

🔒 **J1・J14 の型番は「決めない」と決めた**（2026-09-13・調停役）。**決め忘れではない。**
**転び方が 3 通りあり、そのうち 1 通りでしか型番が要らないため。**

| 転び方 | この行はどうなるか |
|---|---|
| **XIAO を直付け** | 🔒 **行ごと消える**（足の長さは g で選べる） |
| ソケットのまま・口が PH | **1.40 と書ける**（⚠「足 3.0 の品を買う」という買い方の指定とセット） |
| ソケットのまま・口が SMD | 🔴 **このときだけソケットが最大になる**（口 0.00 に対し 1.40）⇒ **型番を決める必要が出る** |

⚠ **3 番目は「口を SMD にしたのに XIAO は直付けしない」という組み合わせで、
直付けは SMD とセットでしか成立しない**（口が PH だと、板を上げ切ってもピンが裏に 0.100 しか出ない）
**ため、たぶん起きない。**⇒ **起きたらその場で決める。**

🔒 **はんだの盛りの上限: 足の先が見える高さまで。**
🔴 **足の先を隠すほど盛ると、継ぎ目が目で確かめられなくなる。**
⚠ とくに XIAO を直付けにする場合、**裏へ出るのは 0.5 しかない**（g 3.9 の選択）。
**0.5 は「足の先が出る量」で「はんだ代」ではない。**はんだはその足の周りにランドから円錐を作るので、
**盛りが 0.5 を越えると足の先が埋まる**。⇒ **埋まった継ぎ目は、戻れない場所に作った見えない継ぎ目になる。**
（2026-09-13。g 3.9 を採った理由そのものが「継ぎ目が見える」だった）

🔒 **組む順が 1 つ決まる: 線を挿してから電池を載せる。**
`gen_pcb.py` のプラグの通り道の検査が、**J2・J4・J5 の 3 つは通り道が電池の下**だと出す。
電池の底 4.8 と PH の横挿し 4.8 は**面一**なので、指の入る隙間が無い。
⇒ **この 3 本は電池より先に挿す。挿し直すときは電池を外す。**
（残る J6・J10 の通り道は電池の外なので、いつでも挿し直せる）

位置が先に決まっている物: XIAO の口 2 列（X 9.40 と 24.63・Y 2.90〜18.14）／
AS5600 のチップ（**X 36.2・Y 7.0**）／ USB-C（上の縁・X 31）／ スライドスイッチ（上の縁・X 17）。
（⛔ 最初に届いた表の「AS5600 は Y 27.04」「立入禁止は Y 25〜29」は、板が固まる前の草稿の値。
　実装は `gen_pcb.py` の `PLACE["U4"]` と `KEEPOUT_MAGNET` が正）

✅ **「電池と磁石の柱が当たる」は消えた**（2026-09-13 夜）。電池が Y 24.2〜64.2 へ動き、
磁石の柱は Y 4.0〜10.0 なので **Y が 1mm も重ならない**（X は 3.3mm 重なるが、それだけでは当たらない）。

✅ **「左下の柱が 0.05mm 電池に掛かる」も外れだった**（2026-09-13 夜・筐体側が実寸で当て直した）。
私が見たのは `POSTS` の (3.30, 38.80) の**抜き φ6.5**（板 X 0.05〜6.55）で、電池の左端 6.50 と
0.05 重なっていた。**柱の実寸は φ6.0**（板 X 0.30〜6.30）なので、**電池との空きは 0.20 ✅**。
⇒ 🔴 **`POSTS` の径は「抜き」であって「柱」ではない。**柱 ＋ 片側 0.25 の逃げが入っている。
**この表で当たりを言うときは、必ず「抜きの径で見た」と書いて筐体側に実寸で当て直してもらう。**
（今回はそう書いたので止まった。書かずに渡していたら、電池か柱を動かさせていた）

##### 面積 — 通る

`parts_area.py` で、生成済みの `hub_power.kicad_pcb` から courtyard の外接矩形を測った。

| | 部品数 | courtyard 合計 |
|---|---|---|
| いまの 2 枚（全部） | 55 | **1115mm²** |
| 消えるもの（J11・J12・J3・J7） | 4 | −194mm² |
| 増えるもの（XIAO の口 2 列目・AS5600・パスコン・スライドスイッチ） | 4 | +152mm² |
| **v6 の 1 枚** | 55 | **≈ 1073mm²** |

板 3573mm² に対して **30%**。実績として電源板は 40 × 32 = 1280mm² に courtyard 548mm²
（**43%**）を詰めてあるので、30% は余裕がある。**面積は制約にならない。**

##### 背の高さ — 4.0mm を超えるのは 5 つだけ

| 部品 | 高さ [mm] | 出どころ |
|---|---|---|
| **K31 リレー Omron G6S-2F** | **9.33** | 📄 `hardware/ref/relay_g6s/en-g6s.pdf` 5 ページ ＋ KiCad の STEP。下の 🔴 を読むこと |
| J1 XIAO の口（2.54 のメス） | 8.5 | 筐体セッションの数字 |
| J2 OLED・J6 リード・J8 会話ボタン（2.54 のメス） | 8.5 | 同 |
| J4・J5 スピーカーの JST PH（縦挿し） | **6.0** | 📄 `hardware/ref/jst_ph/ePH.pdf` 3 ページ Top entry |
| J10 電池の JST PH（横挿し） | **4.8** | 📄 同 3 ページ Side entry |

4.0mm に収まるもの（＝電源・充電・電流計の回路は**まるごとここ**）:

| 部品 | 高さ [mm] | 出どころ |
|---|---|---|
| J13 USB-C（TYPE-C-31-M-12） | 3.16 | 二次資料（PCBWiki）・メーカー図は未取得 |
| L1 インダクタ FNR4018S | 1.8 | 型番の「18」＝ 1.8mm |
| C6・C9 47µF 1206（CL31A476MPHNNNE） | 1.6 | ⚠ 一般値・未確認 |
| D31 SOD-123 ／ Q1・Q31 SOT-23 | 1.35 ／ 1.3 | ⚠ 一般値・未確認 |
| U1 VQFN-16 ／ U2 QFN-20 ／ U3 MSOP-10 ／ AS5600 SOIC-8 | 1.0〜1.75 | ⚠ 一般値・未確認 |
| 0805・1206 の抵抗・コンデンサ・LED | 1.45 以下 | ⚠ 一般値・未確認 |

🔴 「一般値」の行はデータシートで取っていない。設計の可否がその 0.1mm で決まるなら取り直すこと。

🔴 **リレーの高さを 2026-09-13 に 7.3 → 9.33 へ訂正した。**私が📄の図を読み違えていた。

Omron の外形図（5 ページ）の正面図は **14.8 が横・9.2 が縦（＝高さ）**で、右の端面図の **7.3 が幅**、
その下の 9.2 +0.5/−0.3 は端子が張り出す幅である。**7.3 は幅であって高さではない。**
KiCad 10 同梱の STEP（`share/kicad/3dmodels/Relay_SMD.3dshapes/Relay_DPDT_Omron_G6S-2F.step`）の
座標を数えると X 9.200 × Y 14.800 × **Z 0〜9.330** で、板の面から測った 9.33 が設計に使う数字。
📄 の 9.2 との差 0.13 は端子の浮きぶん。

⚠ 別の資料にある **5.7 は出どころが辿れないので使わない**（2026-09-13・筐体側の `CASE-V6-PLAN.md`）。

**結論は変わらない。**電池の影（天井 4.8）にはもともと置かないし、Z1 の天井 12.4 に対して
9.33 なら 3.07 の余裕がある。**ただし「XIAO の谷（天井 9.0）へ寝かせる」案は完全に死んだ**
（9.33 ＞ 9.0）。捨てておいて正解だった。

##### 区画ごとの割り付け（2026-09-13・回路側と筐体側で詰めた）

**電池の影（天井 4.8）には、電源・充電・電流計の回路とリレーの駆動がまるごと入る。**
courtyard 372mm² に対して 1200mm² あり、一番背が高いのがインダクタの 1.8 なので余る。

⛔ **「JST PH の横挿しは 4.85 あるので影には置けない」は取り下げる**（2026-09-13）。
数字が 2 つとも動いた。**PH の横挿しは 📄 で 4.8**（`hardware/ref/jst_ph/ePH.pdf` 3 ページ
「Header（Through-hole type）・Side entry type」の 4.8。4.85 は出どころを辿れなかった）。
**影の天井も 4.4 → 4.8 になった**（下記）。⇒ **ちょうど面一で、影の下でも成り立つ。**

口は**全部 L 字のメスソケット**にする（線が板の面と平行に抜ける。筐体側の条件・2026-09-13）。
ただし **XIAO の口だけは縦**（ReSpeaker のパッドが真上から挿さるため）。

**L 字の実寸**（KiCad 10 の足形から courtyard を実測。`parts_area.py --fp` で取り直せる）

| 口 | L 字（Horizontal） | 縦（Vertical） |
|---|---|---|
| 1×7（XIAO） | 12.30 × 18.80 ＝ 231 | 3.54 × 18.78 ＝ **67**（こちらを使う） |
| 1×4（OLED） | **12.30 × 11.15 ＝ 137** | 3.54 × 11.16 ＝ 40 |
| 1×2（リード・会話ボタン） | 12.30 × 6.10 ＝ 75 | — |
| JST PH 横（スピーカー・電池） | 6.90 × 8.60 ＝ 59 | — |

🔴 **L 字は板の奥へ 12.30mm 食う。**しかも 1×4 は横幅も 11.15 ある。

🔴 **2026-09-13 に私の読み違いを訂正した。**「右の帯（Z3）は幅 9.0」は**電池が居る Y の範囲
だけの話**である（幅を絞っているのは電池）。**電池より下では X 26〜44 が地続きの 18mm 幅**になる。
私はこれを「上から下まで幅 9 の帯」と扱って、Z1 の詰まりを 63〜69% と**悲観側に外して**報告していた。

**背の高い物（天井 4.8 に入らない物）を置ける面積**で積むのが正しい
（🔒 2026-09-13 夜に電池が X 6.5〜36.5・Y 24.2〜64.2 で確定した版）:

| Y の範囲 | 置ける幅 | 天井 | 面積 |
|---|---|---|---|
| **Y 0〜24.2**（電池より下） | **44 全部**（谷 X 10.67〜23.36 だけ 9.0） | 12.4〜14.2 | 1065mm² |
| Y 24.2〜64.2（電池の右） | X 36.5〜44 の **7.50** | 14.2 | 300mm² |
| Y 24.2〜64.2（電池の左） | X 0〜6.5 の **6.50** | 14.2 | 260mm² |
| Y 64.2〜81.2（電池より上） | 44 全部（切り欠き 82 を除く） | 12.4〜17.2 | 748mm² |
| | | | **合計 2373mm²** |

⚠ **面積が増えても、置ける物が増えたわけではない。**電池より上の 748mm² は
J4・SW1・USB-C・R44/R45・柱 2 本・切り欠きで埋まっていて、**PH の 4 ピン（10.90 × 8.60）を
0.5mm 刻みで当てると 0 通り**だった（2026-09-13）。[[check-each-not-the-total]]

背の高い物の合計は **896mm²**（XIAO 133・リレー 164・スピーカー 119・OLED・リード・
会話ボタン・電池・USB-C 100・スイッチ 46・AS5600 47）で ⇒ **51%**。
**OLED は PH の 4 ピンでも L 字のソケットでも、Y 0〜25 に置けば入る。幅 50 は要らない。**

🔒 **OLED の口は PH の 4 ピン横型に決めた**（2026-09-13・筐体側）。
`Connector_JST:JST_PH_S4B-PH-K_1x04_P2.00mm_Horizontal`（JST **S4B-PH-K-S**）・
courtyard **10.90 × 8.60 ＝ 94mm²**・背 **4.8**。理由は面積ではなく 3 つ:

1. 背 4.8 は L 字の 8.5 より低く、置ける場所が Y 0〜25 に縛られない
2. スピーカー IN／OUT・電池と同じ部品の系で揃う（種類が増えない）
3. **PH には鍵がある。**2026-09-11 に OLED を逆に挿して「壊れた」と判定した事故
   （[[symptom-is-not-a-verdict]]・2 章）が構造的に消える

⛔⛔ **この下の「リードと会話ボタンは 2.54 の L 字ソケットのまま」は、板では実装していない。**
🔒 **実物の J6 は PH の 4 ピン横型**（`Connector_JST:JST_PH_S4B-PH-K_1x04_P2.00mm_Horizontal`・
`gen_sch.py` の `part("J6", …, "REED+BTN2")`）。**リードと会話ボタンを 1 つの口にまとめてある。**
理由は口の種類ではなく**数**で、口が 6 つに対してプラグの通り道まで取れる場所が板に **5 か所**
しか無かった。ユーザー「別に線が二股になるのは構わないと思います」（2026-09-13）。
並びは **1 EN・2 GND → リード ／ 3 GND・4 BTN → 会話ボタン**で、隣り合う 2 本ずつに分かれる
（GND を 2 本にして、1 つの端子へ線を 2 本入れることも線を割って継ぐことも避けた）。

⛔ **「口を 6 つとも PH の横型で揃える」案は取り下げた**（2026-09-13・私が出して私が畳んだ）。
リードと会話ボタンに当て直すと、勧めた 3 つの理由が全部落ちる、と書いた。
**下の 3 つのうち 2 番は、そのあと前提が消えている**（記録として残す）。

1. **鍵が効かない。**リードも会話ボタンも無極性で、逆に挿しても何も起きない。しかも
   **PH 2 ピンどうしは互いに挿せる**ので、線を取り違える事故は PH でも防げない。防げるのは
   「4 ピンの口に 2 ピンの線がずれて刺さる」ほうで、**OLED を PH にした時点でもう消えている**
2. ⛔ **背 4.8 でも置ける場所が増えない。**電池の影の天井は 4.4 なので 4.8 は入らない（0.4 足りない）
   → **天井は 4.8 になった**ので、この理由は消えた
3. **面積 32mm² は効かない。**1741mm² に対して 1.8% で、置き方の差に埋もれる

⚠ **OLED の線は両端が違う種類になる**（板の側が PH・OLED の側は L 字ヘッダ＋DuPont のメス）。
工具は両方ある（IWS-2520M・[[crimper-sn58b-not-for-qi]]）。

🔴 **J2 のピンの並びは PH にしても維持する。**`gen_sch.py` の `PORT_FLIP = {"OLED"}` で
**1 GND・2 VCC・3 SCL・4 SDA**（HS154L03W2C01 のパネルの並び）にしてある。

⛔ **「OLED だけ縦のソケットにして線を上で曲げる（8.5 ＋ 3.6 ＝ 12.1）」案は取り下げた**
（2026-09-13）。私が板側のソケットと曲がりしか数えず、**線側のハウジングを数えていなかった**。
`hardware/parts/plug.scad` の鎖（樹脂 2.5 ＋ ハウジング 14 ＋ 曲がり 3.0 ＝ 19.5）が正。

##### 歯車 3 枚案の AS5600 は Z3 の下端（2026-09-13・当ててみて通った）

筐体側の訂正: 読み取りの芯は **板 X 36.2・Y 7**（＝右の帯の一番下）で、**X は動かない**。
軸 φ4 は ReSpeaker の右端（箱 X 36.007）をかわせる X にしか降ろせないため。
⇒ **Z1 には来ない。Z1 は 56% のまま。**

板側で当てた結果:

- AS5600（SOIC-8・7.40 × 5.40）を X 36.2・Y 7 に置くと **X 32.5〜39.9・Y 4.3〜9.7**。
  X 35 をまたぐが Y 25 より下なので電池の影ではない。天井 12.4 に対して 1.75 ✅
- 磁石の φ6 の柱（X 33.2〜39.2）も同じ理由で通る
- **リレーは 90° 回して X 26〜41.3・Y 12〜22.7**（15.3 × 10.7）。AS5600 の上端 9.7 との隙間 **2.3** ✅
  天井 12.4〜14.2 に対してリレー 9.33 ✅

⛔ 前に書いた「AS5600 を Z1 の X 0〜8 か X 26〜33 へ」は、筐体側の訂正で不要になった。
（Z1 の谷 X 10.67〜23.36 に磁石の柱を立てられない — XIAO の胴がそこに居る — という事実自体は生きている。）

##### ⬜ 電池を入れ直す（2026-09-13・調査。**買っていない**）

v6 の筐体でいまの電池（**35 × 50 × 6** で模型化）が柱 3 本・AS5600 のブラケット・ReSpeaker の
裏の部品と **548mm³ 当たる**ことが分かり、入る寸法の品を探した（筐体側からの依頼）。

🔴 **日本の通販で買える「3.7V・JST PH 付き・保護回路入り」のリポは、実質 DATA POWER の
DTP 一族だけ**だった。スイッチサイエンスはほぼ全部 販売終了か売り切れ、**秋月は電池の
カテゴリにリポを置いていない**。扱いは マルツ・千石・共立・モノタロウ（同じ品）。

🔴 **型番と実寸は違う。**型番 `603048` に対して実寸は **30 × 49 × 6**（封止の端まで）で、
**丈が 1mm 大きい**。⇒ **並べるときは千石が出している実寸を使う。**

| 型番 | 実寸 幅 × 丈 × 厚 | 容量 | 値段（税込） | 在庫 |
|---|---|---|---|---|
| **DTP603048(PHR)** | **30 × 49 × 6.0** | **860mAh** | ¥1,800〜1,912 | マルツ 40 即納 |
| DTP443442(PHR) | 34 × 43 × 4.4 | 640mAh | ¥1,538〜1,650 | 34 即納 |
| DTP502535(PHR) | 25 × 36 × 5.0 | 400mAh | ¥1,210〜1,285 | 千石あり |
| DTP523455(DI) | 34 × 55 × 5.2 | 1000mAh | **¥550 特価** | マルツ 476 即納 |
| DTP603450(PHR) | 34 × 51 × 6.0 | 1000mAh | ¥2,352〜2,450 | 41 即納 |
| DTP652533(PHR) | 25.5 × 35 × 6.7 | 500mAh | ¥1,428〜1,580 | 63 即納 |
| DTP673450(PHR) | 34.5 × 52 × 6.9 | 1200mAh | ¥2,462〜2,600 | お問合せ |
| DTP502035(PHR) | 20 × 35 × 5.0 | 300mAh | ¥1,087〜1,200 | 81 即納 |
| DTP902040(PHR) | 20 × 41 × 9.0 | 700mAh | ¥1,428〜1,600 | 22 即納 |

⭐ **効くのは厚みでも丈でもなく幅である。**DTP の幅は **20 → 25 → 30 → 34** と飛んでいて、

| 幅 | 最大容量 |
|---|---|
| 〜25 | **500mAh** |
| **30** | **860mAh**（DTP603048 ただ 1 つ） |
| 34 | **1200mAh** |

🔒 **幅 34 は v6 では通らない**（2026-09-13 筐体側）。幅を決めているのは OLED の柱（箱 X 5.3 と 42.7・
φ6.0）で、**限界の φ3.2（肉 0.30）まで細らせても空きは 34.20**。幅 34 ＋ 逃げ 0.5/片側 ＝ 35.0 に
**0.8 足りない**。⇒ **1000〜1200mAh の 4 品は全部落ちる。**

⇒ ⬜ **候補は DTP603048（860mAh）ただ 1 つ。**これが入れば **86%**、入らなければ次は
幅 25 の帯の **500mAh** で、**860 と 500 のあいだに品が無い。**

⚠ **リポは航空便不可。**🔒「買い物は 1 回にまとめる」に**例外が 1 つ要る**（電池だけ別便）。

⬜ **いまの電池の型番がこの repo に無い。**模型の 35 × 50 × 6 から **DTP603450（34 × 51 × 6・
1000mAh）**と推定しているが、**記録ではない。**実物のラベルを見れば分かる。

##### ⬜ 背面の柱が板を貫く（2026-09-13・**板は止めている**）

🔴 背面の殻の床から立つ柱が**中継基板を貫いて**上の部品まで行く、という条件が 2026-09-13 に出た
（筐体側「これは一度も伝えていませんでした」）。板の座標と要る抜き:

| 柱 | 板の座標 | 抜き | |
|---|---|---|---|
| ReSpeaker × 2 | (31.58, 2.46) (2.66, 60.17) | 🔒 **φ4.0** | 柱を板の帯（箱 Z 3.0〜4.6）でだけ φ3.5 に細らせる |
| OLED × 4 | (3.30, 38.80) (40.70, 38.80) (3.30, 71.80) (40.70, 71.80) | ⬜ φ6.5 | **要るかどうか自体が未決**（下） |

✅ **ReSpeaker の 2 本は φ4.0 で解けた**（当て直して確認。U4 のパッドまで **+0.612**・
板の下の縁まで **+0.460**・左の縁まで **+0.660**）。φ5.5 のままだと U4 のパッドを 0.138 削り、
下の縁を 0.290 欠いていた。**柱の位置は 📄 ReSpeaker 自身の取付穴なので動かせず、
AS5600 も磁石の真下から動かせない**ので、細らせる以外に道が無かった。

⬜ **OLED の柱 4 本は、要るかどうかを筐体側が検討中。**OLED を前の殻から支えれば 4 本ごと消え、
板の抜きも **4 × φ6.5 ＝ 132.8mm²** が消える。⇒ **返事が来るまで板は触らない。**
180mm² を入れて引き直した後で「4 本要らない」になるのがいちばん高くつく。

⛔ **「OLED の柱とハブの取付穴を共締めにする」案は没**（2026-09-13）。柱が板で止まると OLED まで
届かず、柱が板を貫くと板を締める手が無い（🔒 樹脂にねじは切らない）。**1 本の柱で板と OLED の
両方は締められない。**

🔴 **ただし、そこへ行き着いた理由は後から変わった。**共締てを検討していたとき、
**柱と取付穴の芯間が 2.309 しか無く、ねじの頭もナットも並ばないことを誰も数えていなかった。**
⇒ 当時「場所が重なるだけ」と書いたのは**気付いていなかったから**で、「不要だから」ではない。
結論（共締ては成立しない）は変わらないが、**理由は「数えていなかった」が正しい。**

🔴 **その 2.309 が実害になった**（2026-09-13・ユーザーが模型を拡大して発見）。
通し穴 φ2.6 と φ2.2 は半径の和 2.40 で **0.09 重なって 1 つの長穴**になり、
ザグリ φ4 どうしは 1.69 重なり、**M2 のなべ頭 φ3.8 とナット（二面幅 4.0）は並ばない。**
⇒ 🔒 **ハブの上の 2 穴を Y 69.5 → 65.25 へ下げた**（柱まで 6.55）。格子は **37 × 66 → 37 × 61.75**。
⇒ `gen_pcb.py` に**取付穴どうし・取付穴と柱の芯間の検査**を足した。枠の重なりだけ見ていて、
**ねじを回す物どうしの間隔**を見ていなかった。

##### 筐体側で確定したもの（2026-09-13）

- **取付穴 37 × 61.75・M2**。板の X 3.5 と 40.5・**Y 3.5 と 65.25**。φ2.2 ＋ 逃げ 0.5 で
  **φ2.7 の無配線帯**。⛔ **上の 2 つが電池の影に入る話は消えた** ── 4 つとも X が 3.5 か 40.5 で、
  電池の X 6.5〜36.5 の外側（左に 3.0・右に 4.0 空く）。ねじの頭が電池に当たる経路は無い
  （⛔ 最初に出た 37 × 74.2 は上の 2 つがスライドスイッチと USB-C の真上で没。
   ⛔ 次の 37 × 66（Y 69.5）は、ねじの頭とナットが柱と並ぶ間隔を誰も数えていなかった。
   `gen_pcb.py` に穴どうし・穴と柱の間隔の検査を入れて Y 65.25 へ下げた）
- 🔴 **板の上の縁に切り欠き**（板 X 12〜22・Y 73 から上端まで・約 82mm²）。ReSpeaker の J2
  （スピーカーのソケット・トップ型）のプラグが線込み 15 で降りてきて板を貫くため。
  プラグの実寸は板 X 13.04〜20.99・Y 73.91〜80.91 で、四方に 0.9〜1.0 の逃げ
- **充電の USB-C は板 X 37.5**（上の縁・胴 8.94 ⇒ X 33.03〜41.97）。ReSpeaker 自身の USB-C の下（天井 9.0）に入る
- **スライドスイッチは板 X 21.95〜31.05**（上の縁）。切り欠きとの隙間 0.96

courtyard の合計は **1,327mm²**（L 字の実寸で積み直した値）。切り欠き後の板 3,491mm² に対して 38%。
⚠ 合計より **Z1 の 63〜69%** のほうが効く。

##### マスタートグルの置き換え — スライドスイッチ（2026-09-13・**未決**）

トグルスイッチが口（J7）ではなく板実装の部品になる。

🔒 **2026-09-13、B（SHOU HAN MST-12D18G3）に決めた**（筐体側。理由は「A の φ0.8 のピンを掴む
滑り子は、筐体側で新しい機構を発明することになるので避けたい」）。
⇒ **下の「A を選ぶと EN の引き上げに手が要る」は発生しない。R13 200kΩ と C32 0.1µF は
PowerBoost の写しのまま触らない。**

**A: ALPS SSSS811101**（LCSC `C109335`）
📄 `hardware/ref/slide_switch_ssss811101/alps_ssss8_catalog.pdf`（SSSS8 シリーズ・Drawing No.7）

| 項目 | 値 |
|---|---|
| 胴 | 6.7 × 2.6 × **1.4**（端子が両端に 0.5・奥に 1.5 → 実装の占有 7.7 × 5.0） |
| **つまみ** | **φ0.8・胴から 0.9mm 出る・板の面から 1.3mm・C0.2 の面取り** |
| 動く量 | **1.5mm**（胴の長辺 6.7 の方向に滑る・actuator direction は Horizontal） |
| 操作力 | **1.5N** |
| ランド | 6.3 × 約 3.0 ＋ **φ0.9 の穴 2 つ**（ground terminal・板に穴が要る） |
| 回路 | 1 回路 2 接点。② が COM・①③ が両側。② と片方だけ使う |
| 定格 | 0.3A 5V DC ／ **最小 50µA 3V DC**・接触抵抗 70mΩ（初期）・10,000 回 |
| 在庫・値段 | LCSC 在庫 44,535・1 個あたり $0.1489（5 個以上） |
| ⚠ 未確認 | JLCPCB の区分（Basic か Extended か） |

🔴 **これは「レバー」ではない。**筐体側に要るのは、この **φ0.8 のピンを掴んで 1.5mm 動かす滑り子**で、
レバーが通るスリットではない。

**B: SHOU HAN MST-12D18G3**（LCSC/JLCPCB `C49023766`）

| 項目 | 値 |
|---|---|
| 外形 | 9.1 × 3.5 × **6.5**・SMD の横出し（Surface Mount, Right Angle）・本物のレバーが横へ出る |
| 定格 | 100mA 12V・SPDT・10,000 回。**最小電流の規定が無い** |
| 在庫・値段 | 在庫 1,140（薄い）・1 個あたり $0.0733（5 個以上） |
| 外形 | 全長 **9.1**・胴 9.1 × 3.5 × **4.0**（板の面から。胴の下に 0.5 の浮き） |
| **レバー** | 胴から **3.0** 出る・断面 **1.5 × 1.5**・**Z は 2.3〜3.8（芯 3.05）** |
| **動く量** | **2.3** ⚠ 📄 の寸法ではない（下の 🔴） |
| ランド | パッド 1.2 × 2.1 を 2.5 ピッチで 3 つ ＋ 位置決めの **φ0.9 無メッキ穴** 2 つ（6.8 間隔） |
| 置き方 | 板 X 21.95〜31.05・**胴のレバー側を板の上端 Y 81.2 に揃える**（レバーは板の外へ 3.0 張り出す）。足形の原点は **板 Y 76.2**。銅は Y 75.15〜77.25 |
| 🔴 発注前に見る | **区分は Extended**（Basic に代わりは無い・上で確認済み）・**在庫 1,477 は薄い** |


🔴 **動く量 2.3 は図面の寸法ではなく、私が PDF のベクタを測った値**（2026-09-13）。
データシートは動く量を寸法として書いていない。平面図で外形の左右の縁の間隔 185.82 単位・
レバーの幅 30.63 単位で、**外形 9.1 とレバー 1.5 が同じ縮尺 20.42 単位/mm で一致する**ので
縮尺は確か。その縮尺でレバーの芯が部品の芯から **1.15** ずれた位置に描かれており、
2 位置が対称なら 1.15 × 2 = **2.30**。⚠ 「描かれているのが端の位置」という前提が入っている。
確実にするには実物を 1 個買って測るしかない（1 個 $0.079・LCSC から）。

##### ⛔ A を選んだ場合の話（2026-09-13 に B へ決まったので**発生しない**。記録として残す）

A の最小電流は 50µA 3V DC。EN は R13 200kΩ で VSYS から引き上げてあるので、スイッチを流れるのは
VSYS 3.0〜4.4V ÷ 200kΩ ＝ **15〜22µA** で、**下限を割る**。

**私の判断では替えない。** R13 200kΩ は PowerBoost 1000C の写しの一部で、この回路の唯一の裏取りが
「PowerBoost と同じである」ことだから（2026-09-12 ユーザー）。替えるならユーザーの承認が要る。

替える場合の数字（**R13 と C32 はセット。片方だけだと跳ね止めが壊れる**）:

| | 接点電流 | バウンス 0.6ms の間に EN が上がる量 |
|---|---|---|
| いま R13 200k・C32 0.1µF | 15〜22µA 🔴 | 4.4V ×（1−e^(−0.6/20)）＝ **0.13V** ✅ |
| R13 47k・C32 0.1µF のまま | 64〜94µA ✅ | 4.4V ×（1−e^(−0.6/4.7)）＝ **0.52V** 🔴 |
| **R13 47k ＋ C32 0.22µF** | 64〜94µA ✅ | 4.4V ×（1−e^(−0.6/10.3)）＝ **0.25V** ✅ |

TPS61090 の「確実に Low」は 0.4V 以下。跳ね止めの機序は「2 章 ⚠ チャタリング対策の 0.1µF」の
とおり τ＝R13×C32 で立ち上がりを鈍らせることなので、R13 を下げたら C32 を上げる。

増える待機電流は電源 OFF のときだけの 64〜94µA（2000mAh から 2 年以上）で実害は無い。
🔴 **リードスイッチ（J6）も同じ EN⇔GND の線なので同じ話。**ただし今のトグルとリードは 21µA で
✅ 実測が通っている（2 章）ので「下限割れ ＝ 即故障」ではない。効くのは接点が酸化したときの接触抵抗。

**B を選べばこの話ごと消える**（最小電流の規定が無い）。

##### 金額への影響（2026-09-13）

- **AS5600-ASOM（`C79815`・SOIC-8）と スライドスイッチ（`C49023766`）はどちらも Extended。**
  Extended が 9 → **11 種類**になり、**発注 1 回あたり $27.63 → $33.77（+$6.14）**。
  部品そのものは 1 枚あたり AS5600 $0.77・スイッチ $0.079

  🔴 **Basic に替えられないことを確かめた（2026-09-13）。**ユーザー「もしかして BASIC を
  調べない癖ある？」。私はこの 2 つを**決めてから区分を見ていた**ので、調べ直した。

  | カテゴリ | 件数 | Basic |
  |---|---|---|
  | Slide Switches | 5,521 | **無し**（絞り込みの Basic が `is-disabled`） |
  | Position Sensors | 2,569 | **無し**（同上） |
  | 対照: Chip Resistor - Surface Mount | — | **有り**（Basic が有効になる） |

  **調べ方**: JLCPCB のカテゴリのページで `label.el-checkbox` の `is-disabled` を読む。
  そのカテゴリに 1 件も無い区分は選べなくなっている。チェックを入れて Apply を押す操作は
  SPA が受け付けないことが多いので使わない。🔴 **対照を取らないと「読み方が壊れている」と
  区別が付かない**（[[no-fabricated-measurements]]）
- **裏面実装をやると JLCPCB の段取りが片面 → 両面になる**（ステンシルと段取りが増える）。
  ⬜ 金額は未確認。上の案 1・2 が通れば裏を使わずに済む

##### I2C と 3V3（2026-09-13）

筐体セッションから「I2C の引き上げは載せない（ReSpeaker Lite に 4.7kΩ ある）」と来たが、
**この板には INA226 用の R42・R43 10kΩ が既に載っている。**ReSpeaker の 4.7kΩ と並列で
**3.2kΩ**。3.3V の I2C として正常な値なので、**AS5600 のために足すものも減らすものも無い。**

3V3 は XIAO／ReSpeaker から口で入ってくる。AS5600 の 6.5mA がそこに乗る。
⬜ ReSpeaker Lite の 3V3 の余力は非公開で、こちらでは確かめられない（下の「ReSpeaker Lite 側の事実」）。

### ReSpeaker Lite 側の事実

| 項目 | 内容 |
|---|---|
| 入力 | **5V必須。** USB-C ポートと基板上の外部5V給電パッドの2つだけで、3.3V/3.7V を直接入れる口は無い |
| 充電機能 | **無い。** 5Vをもらって動くだけの基板で、電池コネクタも充電ICも載っていない |
| 消費電流 | **非公開。** ReSpeaker Lite も XMOS XU316 も数値が出ていない |
| XIAO の給電 | ボード側から給電される。XIAO 自身の USB-C は書き込み用に別にある（USB-C は2つある） |

**リポで動かすなら昇圧が要る。** 電池3.7V → 昇圧5V → ReSpeaker の5V、という経路になる。

### XIAO の充電回路は使わない

XIAO は充電回路を内蔵しているので「電池を XIAO に繋いで XIAO の USB-C で充電」が一見いちばん
簡単に見えるが、採らない。理由は3つある。

1. 🔴 **充電電流が 100mA しかない。** これが決定的で、本機の消費は待機でも 200〜300mA あるため、
   **繋ぎっぱなしでも充電が追いつかず減っていく**
2. **書き込み用の口と兼用になる。** パソコンに挿すとシリアルとして認識され、DTR/RTS で再起動が
   かかる（`monitor_dtr = 0` を入れているのはそのため）。利用者が充電のつもりで挿す口としては危うい
3. **充電経路が2つになる。** 昇圧のために電池は外付け回路へ繋ぐので、XIAO 側でも充電すると
   同じ電池に充電器が2つぶら下がって制御が競合する

昇圧とパススルーは外付けで用意することが確定しているので、充電もそこへ寄せる。

### PowerBoost 1000C のピン

✅ **実物で確認した（2026-08-14・ユーザーがシルクを読み上げ）。** 下辺に1列8ピン・2.54mmピッチ。
基板の外形は **36.2 × 22.86mm**（USBコネクタは外へはみ出す）。

```
5Vo  GND  LBO  GND  EN  Vsh  LiPo  USB
```

⚠ **GND は 2 本ある。使い分けはこれだけ**: `5Vo` の隣の GND は 5V 出力と対。`EN` の隣の GND はリード／トグルの戻りと対。

| ピン | 役割 | この機体での使い道 |
|---|---|---|
| `5Vo` | 昇圧出力。**約5.2V**（5.0Vちょうどではない） | ReSpeaker への給電 |
| `GND`（5V側） | 上と対のグランド | 同上 |
| `LBO` | 🔴 **既定で BAT にプルアップされている。** 電池が3.2Vを切ると0Vに落ちる | 下記 |
| `GND`（EN側） | 電池・スイッチ側のグランド | リード／トグルの戻り |
| `EN` | 既定でVSへプルアップ。**GNDに落とすと昇圧が止まる**（消費20µA） | リードスイッチ＋トグルの両方 |
| `Vsh` | ロードシェア出力。充電中は約5V、それ以外は電池電圧 | **使わない** |
| `LiPo` | JSTに直結（3.0〜4.2V） | **使わない**（電池はJSTへ直結する） |
| `USB` | micro USB の5V。**充電用の入力で、出力ではない** | 利用者が挿す充電の口 |

充電は最大 1000mA で、1000mAh の電池なら1〜1.5時間。⚠ 充電しながら最大出力で使うと発熱する
ので、**USB電源は2A以上のものを使う**（Adafruit 注記）。

### 🔴 LBO を ESP32 に直結してはいけない

Adafruit のピン説明に "By default it is pulled high to **BAT**" とある。つまり LBO の H レベルは
5V でも 3.3V でもなく**電池電圧そのもの（3.0〜4.2V）**で、✅ 実測でも **4.06V** だった。
**ESP32 のピンは 3.3V までなので、直結すると壊す。**

⚠ **分圧抵抗では解けない。** プルアップ元が電池なので、**電池が減るとH側の電圧も一緒に下がる**。
満充電4.2Vで3.3V以下に収め、かつ3.2V（LBOが落ちる直前）で ESP32 が H と読む 2.48V 以上を保つには、
分圧比が **0.775〜0.786** の幅に入っていなければならない。**1.4% しか無く、5%品では作れない。**

解はトランジスタ1石で受けることで、こうすると ESP32 のピンには電池の電圧が一切かからない。

```
LBO ──[100kΩ]── B
                 2SC1815      C ──→ 空きGPIO（内部プルアップで読む）
                 E ── GND
```

- 電池が元気なうち＝LBO が H＝トランジスタON＝**GPIOはLOW**
- 電池が3.2Vを切ると＝LBO が 0V＝トランジスタOFF＝**GPIOはHIGH**
- ⚠ **論理が反転する。** ファームでは「HIGH＝電池切れ間近」と読む
- ⚠ 100kΩ を 10kΩ にすると待機が 350µA になり、つまみOFF時の 20µA を1桁上回る。**保管中に効く**

🔴 **ただし繋ぐ GPIO が無いので、この回路は宙に浮いている**（2026-08-14）。`D0`〜`D10` の11本すべてに
用途がある（[ARCHITECTURE.md](ARCHITECTURE.md)）。**電池残量は INA226 で読むことにしたので**
（6章）、**この回路は当面使わない。**

---

## 2. 電源の入切 — リードスイッチで切る・AS5600で予告する

🔒 **決定（2026-08-01）。つまみのOFFで完全に落とす**（20µA）。起動は数秒で済むので、疑似電源OFFで
待機電力を垂れ流す意味が無い。**トグルスイッチは非常用**で、日常では触らせない。

### なぜファームで EN を落とせないか

**「マイコンが EN を落として自分の電源を切る」は発振する。**

1. ファームが GPIO で EN を Low に引く → 2. 昇圧が止まり **ESP32自身も死ぬ** →
3. 死んだ GPIO は Hi-Z → EN は基板上で VS へプルアップされ **High に戻る** →
4. 昇圧が再起動 → ESP32 が起動 → つまみは OFF 位置のまま → 1へ戻る

AS5600 化で OFF が「角度の閾値」になったことで、これは致命的になった。可変抵抗の連動スイッチなら
接点が機械的に Low を保持できたが、**角度はファームでしか読めず、そのファームが死ぬ。**
電源ゼロの状態から ON に戻す主体がいない。

### 解: リードスイッチを EN⇔GND に入れる

**電源が無くても効く部品**でなければならない。ホールICは電源が要るので使えない。リードスイッチは
**磁石で閉じる無接触・無電源の接点**で、この条件を満たす唯一の現実的な選択肢である。

```
つまみOFF位置 → 磁石がリードスイッチに近づく → 接点が閉じる → EN=GND → 完全停止(20µA)
つまみを回す  → 磁石が離れる → 接点が開く → ENがプルアップで復帰 → 数秒で起動
```

🔒 **保持は機械（磁石）が担うので発振しない。ファームは EN に触らない**＝GPIO での EN 制御は
実装しない。

### ミュートの順序は AS5600 が担保する

リードスイッチが直接 EN を切るだけでは、再生中にブツ切りになって轟音の穴を踏む。そこを
**絶対角度センサだからできる形**で埋める。**リードスイッチが閉じる数度手前に、ファーム側の
ミュート閾値を置く。**

| 角度 | 起きること | 担当 |
|---|---|---|
| OFF手前（数度） | ミュート・会話終了・停止処理 | ファーム（AS5600の角度） |
| OFF位置 | EN=GND・完全停止 | リードスイッチ（磁石） |

つまみは連続的に回るので、角度が閾値に入った時点で走り切ってから、数度あとに電源が物理的に
落ちる。**順序が構造的に保証される。**

### ✅ 角度差の実測（2026-08-17・つまみを組んでテスタで当てた）

| 項目 | 値 | 出どころ |
|---|---|---|
| リードの境界（OFF位置から） | **約15度** | 📷 写真からの読み取り。つまみの印の向きで測定 |
| ヒステリシス（開く角度と閉じる角度の差） | **見えなかった**（同じ角度） | ユーザー実測 |
| ファームのミュート閾値 `KATANORI_KNOB_OFF_DEG` | **5 → 25** に変更 | 上の実測から。余裕 10度 |

🔴 **既定の 5度 のままだと順序が逆だった。** リードは 15度 で閉じるので、ファームが
まだ鳴らしている 15〜5度 の区間で EN が落ちる。狙っていたフェードが効かず**ブツ切り**になる。
閾値は**リードより外側（角度の大きい側）**でなければ意味がない。

🔴 **この境界を「実機で `knob` の相対角度を見て詰める」ことはできない。**
一度そう書いたが成立しない（2026-08-17・ユーザーが却下）。
- **落ちる経路そのものを通した測定になっている。** 電源が落ちた時点で観測側も死ぬので、
  最後に出た1行がいつの角度なのか決められない
- `OFF_DEG` より内側では `pct` が 0 のまま動かず、**そもそも表示が更新されない。**
  出てくるのは閾値の 25度 であって、リードの境界ではない（自分で入れた数字を読むだけ）

✅ **正しいのは、いま採った方法そのもの。** テスタをリードの足に直接当てる測り方は、
電源にも ESP32 にも依存しない**独立した観測**になっている。
残る誤差はリード側ではなく**角度の読み取りが写真であること**だけなので、詰めるなら
つまみの向きの測り方（分度器など）を上げる。⚠ 余裕を 10度 取ってあるので、
±数度 の読み取り誤差はこの閾値を揺らさない。

🔒 リード用磁石の座る高さは刷り直しでも据え置いてある（`knob_v5.scad` の `MAG2_SEAT_Z`）。
**この 15度 は刷り直しても持ち越せる。**

⚠ **これで塞がるのは「つまみを回して切る」経路だけ。** 再生中の電池切れ・クラッシュ・
ウォッチドッグ・ブラウンアウトは守れない（3章のミュートリレー）。

### ⚠ チャタリング対策の 0.1µF は EN⇔GND に入れる

リードスイッチは接点が閉じる瞬間に数回跳ねる（実測仕様で**バウンス0.6ms**前後）。EN に直結すると
昇圧が1ms弱パタつき、ブラウンアウトで中途半端なリセットを踏む余地が残る。**EN⇔GND に 0.1µF を
1個入れる。**

⚠ **効いているのは立ち上がり側である**（2026-08-14 訂正）。リードが閉じる瞬間はコンデンサがスイッチで直接放電
されるので**立ち下がりは鈍らない**。効くのはそのあとで、**跳ねて一瞬開いたときに EN が復帰しきらない**
ことによる。EN は基板の中でプルアップされているので、開いている間の立ち上がりは
τ＝プルアップ抵抗×0.1µF で鈍る。バウンス0.6msに対して十分に遅いので昇圧は再起動しない。
**この機序なので、容量を減らすと効かなくなる。**

### ⚠ 磁石が2つになる（AS5600 への干渉）

**これがこの方式でいちばんの risk。** AS5600 は軸上の磁石の**磁界の向き**を読んでいるので、
リードスイッチ用の磁石が近いと角度がずれる。AS5600 の磁石は**軸の中心**、リード用の磁石は
**つまみの外周**へ置いて距離を稼ぐ。ずれたかどうかは `STATUS`(0x0B) の MD/ML/MH と `AGC`(0x1A) を
見れば数字で分かる（[KNOB-ENCODER.md](KNOB-ENCODER.md)）。

🆕 **v6（歯車 3 枚）で前提が変わった**（2026-09-13・調停役の裁定 #14 の文面を、板の持ち主が入れた）。
🔴 **上の段落は消さない。**AS5600 が**つまみと同軸だった頃**の記述で、
**なぜ距離を稼いだのか**を失うと、次に磁石を動かす人が同じ穴に落ちる。

| | v5 まで | v6 |
|---|---|---|
| AS5600 の芯 | つまみと同軸 | つまみから **24.56mm**（歯車 3 枚で逃がした） |
| つまみ外周の磁石 ↔ AS5600 | 数 mm | **24.56 − r ＝ 12.81mm**（🔒 r ＝ **11.75**） |

🔒 **r ＝ 11.75**（2026-09-13 調停役。リードは皿の下・箱 X 11.50 へ移り、前の壁を 1.20 彫る形で決着）。
⚠ **式は残す**（`24.56 − r`）。磁石の半径を動かす人が計算し直さずに済むように。

⚠ **この数は 3 回動いている。**書き換えるときは経緯ごと動かすこと。

| いつ | r | 距離 | どうなったか |
|---|---|---|---|
| 2026-09-13 | 8.4（皿の裏） | 16.16 | ⛔ **真下にリードの座が入らなかった**（前の壁の内面 21.00 と ReSpeaker のマイク面 18.85 のあいだが 2.15 しか無く、座は v5 の実績で 3.30 角。つまみの芯から r 12.0 まで全周がこの帯） |
| 同日 | 12.45 | 12.11 | デザイン側の検査待ちのまま置き換わった |
| 同日 | **11.75** | **12.81** | 🔒 リードを皿の下へ移して決着 |

🔴 **この risk は閉じない。**v6 の磁気源を近い順に並べると、

| 磁気源 | AS5600 の芯から |
|---|---|
| ミュートリレー K31 のコイル | **11.95mm**（板側でこれ以上離せない） |
| **リード用磁石** | **12.81mm** ⇒ **リレーとほぼ並ぶ** |
| （v5 のとき） | 数 mm |

⇒ 歯車 3 枚で**遠くはなった**が、**「いちばんの risk ではなくなった」とは言えない。**
リレーとリード用磁石がほぼ同じ距離に並んでいる。⇒ 初回の板で測るのは下の 2 本の掃引で、
**リードの磁石が付いた状態で取ること**（外して取ると片方しか見えない）。

⚠ **v6 で磁気源が 1 つ増えた: ミュートリレー K31 のコイル。**AS5600 の芯から **11.95mm** で、
**板側ではこれ以上離せない**（背 9.33 の物が置けるのは Y 0〜24.2 の帯だけ・上は電池の影で天井 4.8・
右の帯は幅 7.5 に対しリレー 10.7・左は XIAO の口）。

⛔ **この「これ以上離せない」は 2026-09-13 に成り立たなくなった。取り消す（消さずに残す）。**
🔴 **成り立たなくなった理由は板が変わったからではなく、前提が部品に乗っていたから。**
「離せない」の根拠は **G6S-2F の枠 15.30 × 10.70** で、**リレーという機能**の性質ではなかった。

    板の全面に当て直した結果（2026-09-13・他の部品・柱・取付穴・切り欠き・5 口の相手の胴を入れて）
      G6S-2F（15.30 × 10.70）  置ける所 **23 通り** ・ AS5600 との芯間は最大 50.47
      G6K-2F-Y（10 × 6.5）      置ける所 **343 通り**・ **芯間は最大 52.56**
      いまの置き方の芯間                              **11.41**

⇒ **`G6K-2F-Y-TR DC5`（`C47190`・背 5.2）に替えれば、芯間は 11.41 → 52.56 まで取れる**（4.6 倍）。
**コイルも 30mA → 21mA（100mW・237Ω）**なので、磁気源としても弱くなる。
⚠ **替えるかは調停役の裁定待ち。**⭐ **記録としては「部品を替えれば離せる」が正しい。**
🔒 **教訓: 「これ以上できない」と書くときは、効いているのが機能か部品かを分けて書く。**
⚠ 電池より上（Y 64.2〜81.2）も当たってある ── **PH の 4 ピン（10.90 × 8.60）ですら 0 通り**なので、
リレー（15.3 × 10.7）は入らない。

- ⭐ リレーは**動かない**ので、つまみの角度に依存する誤差にはならない。**回るのはリード用磁石のほう**で、
  そちらが悪い種類である
- 🔴 ただしリレーは**無操作 180 秒で開き、操作で閉じる**（`main.cpp` 1271 行と 1316 行・
  `KATANORI_IDLE_SLEEP_SEC` 180）。⇒ 出るのは**居眠りと復帰のたびの段差**で、
  起動時に 1 回取る `knobzero` では消えない
- ⇒ 初回の板で **`AGC` の掃引を リレー強制開（`-DKATANORI_MUTE_RELAY_PIN=-1` でビルド）／
  ふつうのビルド の 2 本**回し、差を見る（手順は [KNOB-ENCODER.md](KNOB-ENCODER.md)
  「初回の板（5 枚）で真っ先に測ること」）
- ⇒ **差が出たら、引く場所はファーム側**（`muteRelayClosed` で場合分け）。板ではない

⚠ **同じ初回に、リードの効きも見る**（2026-09-13 調停役）。機構側は磁石を殻の内面から **2.05** に
置いたが、**リードは内面から 0.58 まで沈められる**（薄壁の下限は `_stl_preflight.py` 386 行の
平たい肉の実績 **0.42**。0.30 は立った壁の値で、ここでは使えない）。
⚠ **具体的な数は磁石の半径が決まってから数え直す。**r が谷（13 前後）になると、
上限を決めるのは溝ではなく前の壁そのものになる。⇒ **テスタをリードの足に当てて、
つまみを回しながら「閉じる箇所が 1 つだけか」を見る。**リレーの掃引 2 本と同じ 1 回の組み立てで済む。

### トグルスイッチ（非常用）

**リードスイッチと並列**に、同じ EN⇔GND へ入れる。どちらか一方でも閉じれば止まる。つまみが
どの位置にあっても、ファームがハングしていても落とせる。

> 「なにがあっても落ちる」を突き詰めるなら電池の JST 線を切る手もあるが、PowerBoost では
> **そこが充電器と共用**なので、OFF のまま充電できなくなる。EN を落とせば TPS61090 が停止するので、
> 実用上はこれで足りる。

🔒 **EN の節点はハブ基板に載せる。** `EN` ⇔ `GND` には**リードスイッチ・マスタートグル・0.1µF の
3つが並列に入る**ので、板の上に節点を作って、外に出る2つはコネクタで受ける。

---

## 3. ミュートリレー — 耳を守る仕組みをソフトに依存させない

🔒 **決定（2026-08-03）・実装と実機合格（2026-08-08）。**
アンプ出力とスピーカーの間にリレーの接点を入れ、**無通電で開**（ノーマルオープン）にする。
閉じるのはファームが起動し切ってから。

**決め手は再生中の電池切れである。** アンプの消費がピークなのは喋っている最中なので、電池が
力尽きるのも「よりによって再生中」になる。しかも電圧が落ちきらない領域では **ESP32だけが
ブラウンアウトでリセットを繰り返し、コーデックは通電したまま解除状態を保持する**ため、
ROMブートログの爆音が連続で出るループに入り得る。爆音自体がさらに電流を食うので悪化方向に回る。
**肩に載る＝耳から数cm**でこれが起きる。2章の EN 制御が守るのは「意図した電源断」だけで、
この経路は守れない。

| 条件 | 理由 |
|---|---|
| ノーマルオープン（無通電で開） | ESP32のGPIOはリセット直後フローティング。駆動側にプルダウンを置き、**無電・リセット中・クラッシュ中は必ず開**にする |
| **ラッチングリレーは使わない** | 消費電流では有利だが、電源が落ちても閉じたまま保持する＝防ぎたい状態そのもの。ここは譲れない |
| 閉じるのは `setup()` の最後 | 落とすときは逆順（ミュート → リレーを開く）でポップを避ける。ただし非常時の保証はリレー単独で成立する |
| 駆動はトランジスタ経由 | GPIO直結では駆動できない。フライバックダイオードを入れる |

⚠ **コイルは動作中ずっと電流を食う**（5V 30〜50mA前後）。1000mAh の電池に対して無視できない量なので、
稼働時間の見積もりに必ず入れる。

### ファーム側の実装（`main.cpp` の `muteRelaySet()`）

| いつ | 何をするか |
|---|---|
| `setup()`（I2S初期化とコーデックのミュートの後） | 接点を**閉じる**。⚠ 行として最後ではない。最終行に置くと**Wi-Fi設定モードだけ音が出ない**（手前で return するため） |
| つまみOFF | **コーデックをミュート → 接点を開く**の順。逆にすると鳴っている最中に切れてポップが出る |
| つまみON | **接点を閉じる**（コーデックはミュートのまま＝乾いた開閉）。解除は `pumpOutputGate()` が鳴らす直前 |
| 意図した再起動・OTA | `muteBeforeRestart()` でミュートの後に開く |
| 音声の初期化に失敗 | **閉じない。** 鳴らせないのに繋ぐ理由が無い |

⚠ **このコードは異常時を守っていない。** 守っているのは基板側の 10kΩ プルダウンで、リセット・
書き込み中・クラッシュ・ブラウンアウトでピンが浮けば**接点は勝手に開く**。ファームは
「普段どおり動いているときに閉じる」だけを担当している。

### 回路

```
              +5V ●──────┬────────────┬──────────────┐
                         │            │              │
                       0.1µF     コイル 167Ω      1N4148
                        ═╧═      Y14H-1C-5DS     ───┬───  ← 帯(カソード)
                         │        (29.9mA)        ／ ▲ ＼    が +5V 側
              GND ●──────┘            │           ───┴───
                                      │              │
                                      └──────┬───────┘
                                             │
                                             ● C（コレクタ）
                                            ╱
   D3(GPIO4) ●────[ 1kΩ ]────●──── B ───┤  2SC1815-GR (NPN)
                             │            ╲
                             │             ● E（エミッタ）
                         [ 10kΩ ]          │
                             │             │
              GND ●──────────┴─────────────┘
```

```
  ReSpeaker Lite                                  スピーカー
   スピーカー出力(+) ●──── COM        NO ────────● (+)
                              ╲
                               ╳  NC ── 何も繋がない（開放のまま）

   スピーカー出力(−) ●───────────────────────────● (−)   ← こちらは切らない
```

**COM–NO を使う**（COM–NC ではない）。⚠ **これが安全の本体**で、コイルに電気が来ていない間は
接点が開き、スピーカーが物理的に切り離される。**NC に繋ぐと「電源が無いとき鳴る」回路になり、
安全が丸ごと裏返る。**

| # | 部品・端子 | 相手 |
|---|---|---|
| 1 | XIAO **`D3`(GPIO4)** | 1kΩ の片足 |
| 2 | 1kΩ の反対側 | 2SC1815 の **B（ベース）** |
| 3 | 10kΩ | **B ⇔ GND** |
| 4 | 2SC1815 **E（エミッタ）** | GND |
| 5 | 2SC1815 **C（コレクタ）** | リレーのコイル片側 |
| 6 | リレーのコイル反対側 | **+5V** |
| 7 | 1N4148 | コイルに**並列**。**帯(カソード)を +5V 側**、アノードを C 側 |
| 8 | 0.1µF | **+5V ⇔ GND**。板のどこに置いてもよい（下記） |
| 9 | リレー **COM** | スピーカー `IN(+)` コネクタ（4章） |
| 10 | リレー **NO** | スピーカー `OUT(+)` コネクタ（4章） |

⚠ 駆動ピンは **`D3`(GPIO4)** である。もともと `D10`(GPIO9) を選んでいたが、**`D10` は ReSpeaker 側が
XMOS の MCLK に使っていて、電源を入れただけでリレーが閉じた**（2026-08-08 実測で確定・
[ARCHITECTURE.md](ARCHITECTURE.md)）。🔴 **教訓は「ファームで未使用」と「電気的に空いている」は
別だということで、選んだピンは必ず実機で検証する。**

### 数字の裏取り（なぜこの定数か）

| 項目 | 計算 | 判定 |
|---|---|---|
| コイル電流 | 5V ÷ 167Ω = **29.9mA** | 2SC1815 の Ic max 150mA に対し余裕 ✅ |
| ベース電流 | (3.3V − 0.8V) ÷ 1kΩ = **2.5mA** | GPIO の負担として軽い ✅ |
| 必要な hFE | 29.9mA ÷ 2.5mA ≒ **12** | GRランクは hFE 200〜400。**深く飽和する** ✅ |
| 10kΩ が食う電流 | 0.8V ÷ 10kΩ = **80µA** | 無視できる ✅ |
| 接点に流れる電流 | 8Ω 2W → **0.5A rms** ⚠ **2W は出どころ不明**（下の「音量の上限」の節） | 接点定格 1A 以内 ✅ ⭐ **1W でも 5W でも 1A の内側なので、この判断は 2W に依存しない** |

🔴 **10kΩ（プルダウン）がこの回路で一番大事な部品である。** ESP32の GPIO はリセット直後・
書き込み中・クラッシュ中に**フローティング**になる。10kΩ が無いとベースが浮いてリレーが不定になり、
**守りたい状況でスピーカーが繋がる**。⚠ **10kΩ は必ずリレー基板側に置く**（ESP32側に置くと、
信号線が抜けたときにベースが浮く）。これで**線が外れても必ず開**になる。

**接点は「乾いた状態」で開閉する。** 順序が「コーデックをミュート → リレーを開く」なので、電流が
流れている最中に接点が切れることはない＝アークがほぼ出ない。定格1Aに余裕がある理由でもある。

### 0.1µF の位置は効かない

「パスコンはリレーの近くに」は一般論としては正しいが、**この回路では効かない。板のどこに置いてもよい。**

| | 数字 |
|---|---|
| 0.1µF が 5V で蓄える電荷 | **0.5µC** → コイルの 30mA なら **17µs** で尽きる |
| コイルの電流の立ち上がり | τ = L/R。信号リレーのコイルは数十〜数百mH なので **0.3〜3ms** |

**コイルが電源から電流を引く速さは、0.1µF が効く速さより2桁遅い。** インダクタ自身が di/dt を
抑えているので、近くに置いても遠くに置いても差が出ない。

🔴 **速い現象は 1N4148 の側にある。** OFF時の逆起電力は**コイル ↔ ダイオードのループに閉じ込め
られていて、コンデンサを通らない。** だから「**ダイオードは裏でコイルの直近**」という指定は本当に
効くし、「コンデンサをリレーの近くに」は効かない。⚠ この2つを混同しないこと。

⚠ **カチカチ発振（電源の落ち込み）に効くのも 0.1µF ではない。** あれは ms オーダーなので
**10〜100µF の電解**が要る。0.1µF を増やす方向は無駄である。

### 部品の向き

| 部品 | 向き |
|---|---|
| **2SC1815-GR** | ⚠ **平らな印字面を手前にすると左から E–C–B**（＝奥に向ければ B–C–E）。**EとBを逆にすると動かない。** 逆向きだと B–E が逆バイアスになるので**熱すら出ない**。見分け方は「**エミッタの足が GND に落ちているか**」 |
| **1N4148** | ⚠ **帯（カソード）が +5V 側。** 逆だとコイルを迂回して +5V→ダイオード→トランジスタが**短絡**になる。⚠ **このとき、トランジスタがOFFならコレクタが開放で電流が流れないので熱くならない。「鳴らない・熱くもない」を理由にダイオードを容疑から外さないこと**（2026-08-08 に半日溶かした） |
| **1N4148 の場所** | 🔴 **半田面（裏）で、コイルの2本に直接またがせる。** 表はリレー本体の下なので置けない。裏なら**ループ面積も最小**。⚠ **接点の2本に架けない**（接点をショートする） |
| **10kΩ** | 足が **GND** に落ちること ← ★安全の要 |

### リレー Y14H-1C-5DS のピン

📄 出典: [akizukidenshi.com/goodsaffix/y14h.pdf](https://akizukidenshi.com/goodsaffix/y14h.pdf)（Y14シリーズ）
＋ [秋月 101346](https://akizukidenshi.com/catalog/g/g101346/)。メーカーは HSIN DA PRECISION。

Drilling Plan より **6-Φ1 の穴**、**横 2.54 と 7.62**、**縦 5.08**。つまり**2列 × 各3本**で、すべて
2.54mm グリッドに乗る。✅ **ユニバーサル基板にそのまま直挿しできる。**

**部品面（表）から見て:**

| 3本の並び | ピン |
|---|---|
| **端の1本**（7.62 側＝離れている方） | ● **COM** ×2 |
| **真ん中** | ● **コイル** ×2 |
| **もう一方の端**（2.54 側＝近い方） | ● **NC**（使わない） / ● **NO**（使う） |

🔴 **データシートの Drilling Plan は Bottom View（裏から見た図）で「2.54 → 7.62」の順なので、
部品面では「7.62 → 2.54」に反転する。** 外形図の Front View が「7.62 → 2.54」になっているのが裏取り。
Internal Connections でコイルの長方形が左から2列目の上下2本に繋がっていて、可動片が上に繋がって
いる（＝NCが上・NOが下）。内訳は コイル2 + COM2 + NC + NO = 6本で余りピンは無い。

✅ **挿し間違えようがない。** 穴の並びが **3つ飛び → 1つ飛び**と非対称なので、180°回すと穴に
入らない。**6穴に挿さった時点で向きは一意に決まる。**

🔒 **2026-08-07 にユーザーがデータシートから読み取って確定させた。以降このピン配置を疑い直さないこと。**

⚠ **コイル以外に 5V を当てないこと**（接点ペアに掛けると短絡）。

### データシートで、設計に効くもの

| 項目 | 値 | 効き方 |
|---|---|---|
| **感動電圧** | **4V** | 🔴 **これが1kΩを選んだ理由の裏付け。** コイルに掛かるのは 5V − Vce(sat)。深く飽和していれば Vce(sat)≒0.1〜0.2V で**コイルは約4.8V**＝余裕0.8V ✅。⚠ ベース抵抗を大きくして飽和が浅くなり Vce=1.5V などになると**コイルは3.5V＝感動電圧を割って吸着しない** |
| 開放電圧 | 0.5V | トランジスタOFFでコイルは0Vなので確実に開く ✅ |
| 接点材質 | **金メッキ銀パラジウム** | 微小電流のドライスイッチング向き。この用途にちょうど合う ✅ |
| 寿命 | 機械 1×10⁷ / 電気 1×10⁵ | 会話ごとに1回開閉しても桁で足りる ✅ |
| 接点定格 | 1A/24VDC・0.5A/125VAC・最大62.5VA 30W | 8Ω2W = 0.5A rms に対して余裕 ✅ |
| 動作/復帰時間 | ともに ≤5ms | 起動シーケンスに影響しない ✅ |
| 外形・重量 | 12.6 × 7.6 × 10mm・**約2.2g** | 肩に載せる機体として無視できる ✅ |
| 使用温度 | −30〜+70℃ | 体温に接する筐体でも余裕 ✅ |
| 耐衝撃 | 誤動作 10G(11ms) / 破壊 100G(6ms) | ⚠ **落とすと一瞬接点が跳ぶ可能性はある。** ミュート中なら実害は小さい |

**コイル側と接点側は電気的に分離している**（絶縁抵抗100MΩ以上・耐電圧1000VAC 1分）。コイルは
電磁石で、金属板を力で動かすだけである。そのため **接点に何も繋がなくてもリレーは動作する**ので、
「カチッと鳴るか」の試験はスピーカーを繋がない状態でできる。
🔒 **この分離こそが、半導体スイッチではなくリレーを選んだ理由である。**

### ✅ 実測で成立している（2026-08-08）

⭐ **`gN` の導通チェッカーより、テスターで接点を直接測るほうが速くて確実だった。**
接点側はコイル側と絶縁されているので、**通電したまま COM–NO の抵抗を測ってよい。GPIOを1本も使わない。**
プローブを COM と NO に当てたまま `high 3` / `hiz 3` を交互に打つ。

| 打つ | 実測 | 意味 |
|---|---|---|
| `hiz 3`（駆動なし） | ✅ **OL** | **接点が開いている＝安全の本体** |
| `high 3` | ✅ **0Ω** | 接点が閉じた |

🔒 **「電気が来ていなければスピーカーは物理的に切り離される」が、コイルではなく接点そのもので
確認できている。** 駆動側も実機8項目に合格済み（[TODO.md](TODO.md)）。

🔒 **この実測は現行の ReSpeaker Lite（2枚目・生きている板）で取っている。** 死んだ1枚目の実績では
ないので、**機体としての実績は有効である。**
⚠ **ただし駆動回路が載っているのは試作のユニバーサル基板（②）で、ハブ基板（③）では配線を
作り直すことになる。回路と部品は同じものを載せ替えるが、配線は別物なので、組んだら
`hiz 3` / `high 3` の OL ⇔ 0Ω をもう一度取ること。**

⬜ **残るのはスピーカー線を IN/OUT に通すことだけで、それは PH2.0 のポート待ち。**

⚠ **挿す前の3点確認**: ① 1N4148 の帯が +5V 側 ② 2SC1815 のエミッタの足が GND の穴に載っている
③ 10kΩ の足が GND に入っている。（穴番号での確認は [relay_board.html](../hardware/parts/relay_board.html) の
「半田付け前の確認」）

⚠ **スピーカーは最後まで繋がない。音が出る段に入る前に必ず声をかけること。**

---

## 4. ハブ基板

**板は 両面スルーホール C基板 DAISEN PU52X74（外形 52 × 74mm・27列×19行）。**
▶ **穴の割り当て・配線の経路・組む順番は [relay_board.html](../hardware/parts/relay_board.html)。**

### 板の外に出る口（境界）

**この基板の境界はこの10口で全部である**（下の表は9行だが、スピーカーの IN / OUT が
2口なので口の数は10）。ここだけ守れば、板と相手側を別々に作れる。**導体は 37 本**で、
うち30本がこの口を通り、残りは電池まわり4本（電池⇔INA226⇔PowerBoost）と同軸1本、
そして筐体の会話ボタンへの2本である。

✅ **口の「出る向き（exit）」は、もう気にしなくてよくなった**（2026-08-17 夜）。

いきさつはこうである。v1 では exit が relay_board.html の**平面図の画面方向**のまま書かれて
いて、板を立てた回転に追従せず、**10口すべてが線の逃げ 25mm を箱の外に要求していた**
（`hardware/frozen/v1-v4/case_v1.scad` の 🔴・17,844mm3）。当初はこれを「向きを行き先から決め直す」
問題だと考えていた。

**しかし v2 で板を床に寝かせたことで、問題ごと消えた**（[case_v2.scad](../hardware/frozen/v1-v4/case_v2.scad) ／ [CASE-V2.md](CASE-V2.md)）。
ヘッダは真上を向いて立ち、線の逃げは**箱の高さ**から取れる。板の縁がどちらを向いていようと
関係がない。⇒ 🔒 **穴の位置もネットの割り当ても exit も、いまのままでよい。**
実測でも、線が箱の外へ出た量は **0mm3**（v1 は 17,844mm3）。

🔒 **2026-09-12 ユーザー「要らないね」: 起こした板（`hardware/pcb/`）には、板の上の直挿しの会話ボタンを
載せない。** 会話ボタンは天板のもの 1 つだけになり、口（`BTN2`）で受ける。いま使っているユニバーサル基板の
側は下の 2026-08-17 のままで、変わったのは新しい板の上だけである。

✅ **`BTN2`（`P26`/`Q26`）はこのままでよい**（2026-08-17 判断）。左へ移せば配線は短くなるが、
赤0 を通っている引き回しをやり直す手間に見合わない。🔒 **この件はもう蒸し返さない。**
⚠ **2026-08-19 の更新**: 板は筐体へ 180度 回して載せることになった（[DIMENSIONS.md](DIMENSIONS.md) 0.6）。
この口は板の上では列26（右端）のままだが、**箱の中では左（X 6.5）に来る。**
会話ボタン（天面の X 20 付近）と同じ側なので、**もう箱を横断しない**
（[CASE-V2.md](CASE-V2.md) では左の通路 X 16.5 を通っている）。判断そのものは変わらない。

| 口 | 並び（左→右 / 上→下） | 相手 |
|---|---|---|
| **XIAO 7本**（ピンヘッダ・DuPont） | 5V / D3 / GND / 3V3 / SCL / SDA / D2 | XIAO のパッド列ヘッダ |
| **OLED 4本**（同） | GND / VDD / SCL / SDA | 🔒 **2026-08-17 に現物で確定した。** 届いた 2.42インチの並びは `GND・VCC・SCL・SDA` で、この表と同じ順だったので穴割りに変更は無い |
| **AS5600 4本**（同） | SDA / SCL / 3V3 / GND | 治具側モジュール |
| **スピーカー IN / OUT**（PH2.0 ×2） | IN: +/− ・ OUT: −/+ | IN ← ReSpeaker のスピーカーソケット／OUT ← スピーカー |
| **PowerBoost 4本** | 5V / **空き** / GND / EN | ⚠ **2本目は意図的な空き**で、`+5V` と `GND` を隣り合わせないための緩衝 |
| **マスタートグル 2本** | EN / GND | MTS-102。⚠ ON-ON なので**コモンと片側だけ**を配線する |
| **リードスイッチ 2本** | GND / EN | つまみへは筐体側で引き回す |
| **INA226 4本** | GND / 3V3 / SCL / SDA | ⬜ 現物の並びを見てから確定 |
| **会話ボタン 2本** | GND / D2 | 筐体の天板側のスイッチへ。🔒 **板の上の直挿しのボタンは維持したまま、この口を足した**（2026-08-17・ユーザー決定）。⚠ **2つは並列**なので、どちらを押しても D2 が GND に落ちる |

🔒 **線の色（全ての口で統一）: `SCL` = 緑・`SDA` = 黄色**（2026-09-02 ユーザー）、**会話ボタンの信号 `D2` = 紫・`3V3` = 赤・`GND` = 黒か白・`EN` = 橙・`5V` = 茶**（2026-09-06 ユーザー）。束をほどかずに線を見分けられる。ユーザーが決めていないのは `D3`（ミュートリレーの駆動）・スピーカー ±・電池 ± で、マニュアル（hardware/tools/manual_v5/_asm_manual_v5.py の COLOR）では仮に灰・青/白（スピーカーのリードと同じ）・赤/黒（電池のリードと同じ）にしてある。

🔒 **2026-08-24（ユーザー）: OLED と AS5600 の口は役割を入れ替えて使う。** 鏡事故の補正として、
OLED 用に計画したヘッダを AS5600 に充て、つまみの線が **OLED から見て右**（このロボットの核心）に出るよう調整済み。
両口とも中身は GND・3V3・SCL・SDA で電気的に同一（並びは物理ヘッダの方に付いて残る）。模型の口の名前は実配線に合わせて入れ替え済み。

🔒 **AS5600 の `DIR` に基板側の穴は作らない。** ハーネスを二股（Yケーブル）にして GND に相乗り
させる（2026-08-11 ユーザー指摘。基板の穴とジャンパが1本ぶん減る）。⚠ モジュールの `OUT` は
繋がない。**`GP0`(PGO) は絶対に繋がない**（OTP書き込み用・焼くと戻せない）。

🔒 **`D0` と `D1` は使わない。どちらも ReSpeaker 側が握っている**（[ARCHITECTURE.md](ARCHITECTURE.md)）。

### 🔴 レイアウトの最優先条件は +5V と GND を離すこと

この板の `+5V` は**機体まるごとの電源**を運ぶ。PowerBoost からの 5V を板で受けることになったので、
行き先は「コイルと 0.1µF」ではなく **XIAO の 5V ピン（＝ReSpeaker 全体）が加わって約 0.5A** になる。

- 🔴 **`+5V` はレール（バス）にしない。点から点へ1本で引く。レールは GND だけ**
- 🔴 **`+5V` は表の被覆線で運ぶ。裏に長く這わせない**（GND と隣り合う距離を作らないため）
- 🔴 **`+5V` と `GND` を隣り合うパッドに置かない。** 2026-08-14 の作図中に PowerBoost の 5V を
  INA226 の GND の隣に置いてしまい、**検査が2件の赤で止めた**（`S13`–`S14` と `R13`–`R14`）

### 配線の規則

**斜めに通さない。交差させない。** perfboard では斜め線は裏で長さが読めず、交差はショートの温床。

- ジャンパは**縦か横だけ**。曲げるときは**直角**に折る
- **レールは半田面（裏）に裸のスズメッキ線、ジャンパは部品面（表）に被覆線。** 面が違うので、
  絵の上で1箇所だけ交差しても短絡しない
- 🔒 **裸のレールは通った穴を全部その電位にする。被覆線は両端しか繋がない。**
  だから節点へ寄せるのは被覆線で、レールで寄せると通り道の穴まで同じ電位になる
- **裸のレールどうしは交差できない**（触れたら短絡）
- 表はリレー本体とコネクタが場所を取るので、レールで埋めない

### 図の物理検査

図は動かすたびに検査が走る。**赤は物理的に成立しない、黄は成立するが作りにくい。**
🔒 **赤がゼロでなければ組まない。**

| 判定 | 内容 |
|---|---|
| 赤 | 1穴に導線2本以上 ／ 別ネットが1つの節点に繋がる（短絡） ／ 同じネットが2つ以上の島に分かれる（断線） ／ 被覆線の全長が10mm未満 ／ 端の剥き線の2穴以内に別ネットのパッド ／ 部品の胴が使用穴を覆う ／ ジャンパが胴の上を通る ／ 抵抗の足の折り返し余裕が2mm未満 ／ ブリッジが隣接していない ／ 板からのはみ出し |
| 黄 | ジャンパどうしの直交交差（被覆線なので短絡はしない） ／ レールの通る穴への挿し込み ／ 部品の胴が穴間より広い |

⚠ **短絡と断線は、宣言ではなく実際の繋がりから判定している。** 端子が宣言するネットを種にして、
レール・ブリッジ・ジャンパ・リレー内部結線をたどって伝播させ、1つのかたまりに2つのネットが
入れば短絡、同じネットが分かれていれば断線と出る。**「繋いだつもり」が図の上で潰せる。**

✅ **赤0・黄0**（2026-08-17 にユーザーが全面的に引き直した版。2026-08-20 にブラウザで再確認）。
OLED を行E へ、AS5600 を列25 へ動かして幹線を引き直したことで、**ジャンパどうしの交差もゼロになった。**

⚠ 初期レイアウトを書き換えたら HTML 内の `SAVE_KEY` の版数を上げる。上げないと前回の保存内容が
読み込まれて新しい初期値が出ない。

### スピーカーは基板の IN / OUT コネクタで受ける

🔒 **決定（2026-08-10・ユーザー指摘）。** `COM` から線を外へ出す形は採らない。基板の中で繋ぐのは
3本だけで、**`IN(+)` → `COM`**、**`NO` → `OUT(+)`**、**`IN(−)` → `OUT(−)`**（(−) は接点を通さず素通し）。

**なぜこの形でなければならないか。** この基板は「外へ出る線は全部コネクタで受ける」と決めてある。
狙いは**引っ張りをコネクタで受けて基板のパッドに掛けないこと**と、**抜き挿しで分解・交換ができる
こと**である。スピーカーは筐体側に付いて線が動く部品なので、`COM` に線を直付けすると引っ張りが
そのまま半田点に乗り、いずれパッドごと剥がれる。IN/OUT の2つを載せれば、ReSpeaker 側もスピーカー側も
コネクタ1本ずつで抜き差しでき、**完成しているスピーカーを切らずに済む。**

⚠ **この設計を最初から持っていなかったのが設計の欠陥である**（2026-08-10 ユーザー指摘）。
🔴 **「板に載らない」を「板に口が要らない」と読み替えると、その部品の口が設計から抜け落ちる。
部品を「載らない」に分類したら、そのぶん口を1つ数えること。**

🔒 コネクタは **PH2.0 2ピン**（ReSpeaker のソケットも同型）。⚠ **PH2.0 は 2.0mm ピッチで、
2.54mm 格子には素直に載らない**（2ピンなので足を開けば届く範囲）。⬜ トップ型かサイド型かは
現物合わせで、**サイド型にすると線が板と平行に抜けるので積み方向の逃げが約10mm浮く。**

🔒 **買った PH の圧着済みペアは、噛み合わせると赤と黒が入れ替わる**（2026-09-11 ユーザーが現物で指摘）。
オス側・メス側とも「黒が左・赤が右」で作られているので、片方を 180° 回して挿す＝**赤が黒に当たる**。
ペアのまま延長線にすると逆接になる。直すときは**片側だけ**ハウジングの窓から爪を起こして端子2本を抜き、
左右を入れ替える（両側やると元に戻る）。線の色は信用せず、電池をつなぐ前にテスターで当たること。

🔴 **これは事故として起きた。**2026-09-11 未明、このペアをそのまま電池線に入れて通電し、
**INA226 から煙が出て、PowerBoost が発熱した。**外して確かめた結果、被害はこの電池の経路だけで止まり、
ハブ基板・ReSpeaker・OLED・つまみ・Wi-Fi・音声は USB 給電で全部動いた（同日 02:31 のシリアルログ）。
この事故の直接の原因は、圧着ペアの色を配線の情報として扱ったこと。

✅ **同日 03 時台に切り分けた結果、焼けたのは INA226 の 1 枚だけだった。**PowerBoost は
発熱したが生きている（EN を外して電池を直結し、`5Vo` に **5.1V** を確認。LED も点灯）。
⚠ **「発熱した」「煙が出た」を死亡と書いたのは早すぎた。**PowerBoost は無事で、
INA226 も煙を見ただけで未検証のままである。試せる部品は試してから交換と書くこと。
同日 03 時台に電池駆動へ戻し、トグルの動作も確認できた。**確認できた被害は INA226 の 1 枚だけ。**

### 🔴 「PowerBoost は 5V を出しているのに機体が起動しない」の切り分け

2026-09-11 の復旧で 30 分使った。原因は **GND の DuPont が外れていた**こと。5V の線は
繋がっていたので、PowerBoost 側では正常な 5V が読めた。[配線の仕様](#配線の仕様)の
「DuPont は緩む前提の接続」の実例である。**電源が来ないときは 5V ではなく GND を先に疑う。**

測る順番はこれでよかった。同じことが起きたらこの順で切る。

| 測る所 | 出た値の意味 |
|---|---|
| ① PowerBoost の `5Vo` ⇔ 隣の GND（負荷を繋いだ状態） | 0V なら保護で止まっている。すぐ電池を外す |
| ② ハブの `XIAO` の口 1本目(5V) ⇔ 3本目(GND) | ここが 0V なら PowerBoost から板までで切れている |
| ③ ハブの `PWR` の口の 5V ピン ⇔ 板の GND | ①が5V・②が0V のとき、線か GND のどちらが切れたかが決まる |

⚠ **`PWR` の口は 4 穴で 3 本目が空きなので、3 本のハウジングが 1 つずれて挿さっても入る。**
測る前に「空きが 3 本目にあるか」を目で見ること。

### ✅ 煙が出た INA226 の切り分け（2026-09-11）

**アドレスは 0x40 ではなく `0x44`。**ファームに実装するときはこの値を使う。
I2C だけ繋いだ状態で機体は正常に起動し、バスも 3V3 も落ちなかった。
`INA` の口の並び（GND・3V3・SCL・SDA）は、この配線のまま応答したので **✅ 確定**。

⚠ **煙を出した個体と新品の差は、手元の道具では出なかった。**

| 検査 | 現品 | 新品 |
|---|---|---|
| I2C で応答（0x44） | ✅ | — |
| バスを止めない・3V3 を落とさない | ✅ | — |
| 外側 2 穴（+ 側・シャント） | 0Ω | 0Ω |
| 内側 2 穴（− 側） | 0Ω | 0Ω |

シャントは切れていない（切れていれば OL になる）。ただし DT10B では 0.1Ω を 0 と区別できないので、
**「切れていない」までしか言えない。**何が煙を出したのかは特定できていない。
🔒 **本番には新品を使い、現品は「2026-09-11 に煙が出た個体」と書いて分けておく。**

🔴 **口ごとに 4 本の並びが違う。OLED とつまみは逆順である。**

| 口 | 板の外から見た並び |
|---|---|
| OLED | GND ・ VDD ・ SCL（緑）・ SDA（黄） |
| AS5600（つまみ） | SDA（黄）・ SCL（緑）・ 3V3 ・ GND |

同じ 4 本で並びだけが逆なので、**挿し間違えても物理的には入る。**同じ夜、OLED がこの向きで
挿さっていて、①I2C バスが止まってファームがスキャンの途中で固まる ②向きを直すと 0x3C で応答するが
画面は真っ暗、という 2 段階の症状を出した。⬜ **どちらかの口の並びを揃えるか、逆挿しできない形にすること。**

### 配線の仕様

| 項目 | 仕様 |
|---|---|
| **線の太さ** | **AWG26〜28 の細い撚り線1種類で全部まかなえる。** 最大は GND の約 **57mA**（コイル30＋OLED20＋AS5600 6.5）で、スピーカー線でも 8Ω2W = **0.5A rms**。AWG26 の短い引き回しなら余裕がある |
| **色の規律** | **黒はGNDだけ。赤は5Vだけ。** ⚠ **3V3に赤を使わない**（5Vと取り違えると OLED のプルアップが I2C を5Vへ引き上げて ESP32 が壊れる）。SDAとSCLは**互いに違う色** |
| **スピーカー線** | **2本を撚り合わせる**（電流ループを小さくする）。⚠ **信号の束と一緒に束ねない** |
| **束ね方** | XIAOからの7本は**1束にまとめて両端を固定**。**基板の穴に通してから半田付け**すると半田に力がかからない |
| **長さ** | ⬜ 筐体ができるまで確定できない。「必要長＋2〜3cmの遊び」。概算の総量は **15本 × 約10cm ＝ 1.5m 程度** |
| **コネクタ** | 🔒 **ハブ基板側にピンヘッダを立てる**（2026-08-07 決定）。XIAO側もパッド列から立てたピンヘッダなので、**ハーネスは両端ともDuPontで同じ形**になる＝新規購入ゼロ。**部品面（表）から挿す**（裏から生やすとハウジングが半田面の裸レールを踏む） |

🔒 **OLED の VCC は 3V3 で確定。5V を繋がない。** モジュールの仕様は 3.3V〜5V だが 3.3V で動かした
実績がある。5V にすると **I2C のプルアップ先が 5V になり**、SDA/SCL が 5V へ引き上がって ESP32 の
入力定格を超えうる。**得るものが無いので検討しない。**

### XIAO 側との接続

✅ **XIAO側の引き出し口は既にある**（[RESPEAKER-LITE.md](RESPEAKER-LITE.md) 5章）:
**XIAO のパッド列から立てた ピンヘッダー7本 × 2列**。新たな加工は不要。

⚠ **このヘッダは高さ10mmで、機体で最も高い。「XIAO面には何も被せられない」。**
効いてくるのは2つで、**ハブ基板を ReSpeaker の上に重ねられない**（横に並べるか別の面に置く）ことと、
**7本の束はこのヘッダから真上に出る**ので筐体の内寸にヘッダ10mm＋線の曲がりを見込むことである。

🔒 **決定（2026-08-07）: DuPont のまま＋抜け止め。はんだ付けはしない。** 分解できることを優先する
（旧ボードが1枚死んでいる経緯があり、XIAO を降ろせる状態を保つ）。

⚠ **いまのままでは持ち歩けない。** 机上の検証中はこのままでよいが、**筐体に入れる前に**両方やる。

| | やること | なぜ |
|---|---|---|
| ① | **束をアンカーに固定する**（コネクタから2〜3cm離れた位置で束を筐体側に結束バンドで留める） | **張力をコネクタに掛けない**のが本体。抜けの原因のほとんどは引っ張り |
| ② | **ハウジングとヘッダの合わせ目にホットボンドを少量** | 振動で少しずつ浮くのを止める。アルコールか加熱で外せるので**分解性は残る** |

⚠ ①だけでは振動に負け、②だけでは引っ張りに負ける。**両方で1つの対策。**

### 部品の高さ — 正は別文書

🔒 **部品寸法の正は [parts.scad](../hardware/parts/parts.scad) と [CASE-V4-LOG.md](CASE-V4-LOG.md) §5 の台帳**
（2026-08-24 に実物で総ざらい済み）。⚠ **この文書に部品寸法を書き戻さないこと。**
2026-08-15 時点の表は 2026-09-01 に削除した（`git log -p docs/POWER.md` で読める）。

電源の側で覚えておくのはこの2つだけである。

🔴 **ハブ基板の最高点はリレーではなくピンヘッダーである**（2026-08-15・ユーザー）。
⚠ **測るのは裸のヘッダではなく、DuPont のハウジングを挿した状態の高さ**
（[DIMENSIONS.md](DIMENSIONS.md) 1.5「コネクタは挿すと伸びる」）。

🔴 **「薄い」は「置き場所が自由」ではない。** 電池は**工具なしで交換できること**が筐体の設計条件
（[TODO.md](TODO.md) B-1）なので、5mm の箱に加えて**抜き挿しの経路と、JST コネクタに指が届く空間**が要る。
⬜ コネクタと線の出る向きは未取得。スピーカーも薄いが**箱（キャビティ）は要る**ので、厚みではなく容積で効いてくる。

---

## 5. 5V の受け口

**道は 1 本で、もう板に組んである。**

```
PowerBoost 下辺 8 ピンの 5Vo / GND(5V側) / EN
   → ハブ基板の PWR の口（5V / 空き / GND / EN・4 ピン）          … 線は ⑪（CASE-V3.md）
   → ハブの +5V ネット
   → ハブの XIAO の口（7 本）の 5V
   → XIAO のパッド列ヘッダの 5V ピン（＝ ReSpeaker の +5V ネット・公式回路図で確認）
```

ReSpeaker の外部 5V パッド（⑨）は**穴の無いランド 2 枚でヘッダが立たない**（[RESPEAKER-LITE.md](RESPEAKER-LITE.md) ⑨）ので使わない。
XIAO の 5V ピンは ReSpeaker の `+5V` ネットに直結（📄 ReSpeaker Lite v1.0 回路図 XIAO ブロック・14 番 → `+5V`。XIAO 側のネット名は `VBUS`）。

⬜ **残る判断は 1 つ**（[TODO.md](TODO.md) A-2）: このヘッダ経由（DuPont・分解できる）で行くか、外部パッドへ線を直に半田するか。**ユーザーが決める。**

### 🔴 制約: XIAO の USB-C と 5V がぶつかる

XIAO の 5V ピンは USB-C の VBUS に直結で、間にダイオードは無い（ショットキー D2 は VBUS → XIAO 内部の VIN 側にだけ入っている）。
⇒ **PowerBoost の 5.2V と PC の 5V が、この経路で直接ぶつかる。**

```
USB-C VBUS ──┬──────────────────→ 5V ピン（14 番）← ここへ PowerBoost の 5.2V が来る
             ├──[D2 ショットキー]──→ VIN ──[U3 降圧]──→ 3V3
             └──[U4 充電 IC]──────→ VBAT
```

🔒 運用で避ける: **PC に挿して書き込むときはマスタートグルを OFF**（昇圧が止まる）。
⬜ 外部パッド ⑨ と ReSpeaker 側の USB-C が `+5V` ネットにどう繋がるか（逆流防止の有無）は公開範囲の外で不明。

## 6. 消費電流・測定・INA226

### 見積もり（推定）

**実測していないので、あくまで桁を掴むための数字。** XMOS の消費が非公開なのが最大の不確定要素。

| 部品 | 会話中(推定) | 待機中(推定) |
|---|---|---|
| XIAO ESP32S3（Wi-Fi常時接続） | 100〜150mA（送信時は瞬間的に300mA前後） | 80〜120mA |
| XMOS XU316（音声処理） | 100〜150mA | 100〜150mA（**常時動いている**） |
| OLED | 10〜20mA | 10〜20mA |
| スピーカーアンプ（音量35%） | 50〜150mA | ほぼ0 |
| **合計（5V側）** | **約300〜450mA** | **約200〜300mA** |

⚠ **スピーカーは未接続の状態で見積もっている。** ここまでの動作確認は3.5mmジャックにイヤホンを
挿して行っており、アンプは一度も動かしていない。接続後に増える。

これを3.7Vのリポから昇圧（効率85%と仮定）して取ると、電池からは1.5倍ほど流れる。

| 電池容量 | 会話が続く時間 | 待機が続く時間 |
|---|---|---|
| 1000mAh | 約1.5時間 | 約2時間 |
| 2000mAh | 約3時間 | 約4時間 |
| 3000mAh | 約4.5時間 | 約6時間 |

⭐ **これを詰めるのに電流計は要らない。** 知りたいのは電流値そのものではなく「1000mAh で何時間
持つか」なので、**満充電から LBO が落ちるまで動かして時計を見る**のが一番直接的で、推定を挟まない。
会話させ続ける版と待機のまま放置する版の2回で、上の表の2列がそのまま埋まる。

⚠ **待機でもあまり減らないのが問題である。** XMOS は常時動いていて眠らない。「使っていない時間が
長い」使い方なら、待機電力が寿命を決める。だから2章の「つまみOFFで完全に落とす」が効いてくる。

🔒 **USB電流チェッカーによる測定は打ち切った（2026-08-14）。** 実際に試したが表示が 0W から動かず、
機体は確実に動いていた。この個体はスマホの充電具合を見る程度の精度の機材で、この機体の
1.5〜2.3W を切り分ける分解能を持っていない。**0W という表示から意味を読み取ろうとしないこと。
この線はもう追わない。**

### INA226 で読む

⭐ **代案として I2C の電力モニタを積む**（2026-08-14・ユーザー提案）。I2C にぶら下がるだけで
**ピンを1本も食わない**ので第1世代の構成のまま載り、**動かしながら消費電流が読める。**

🔴 **定番の残量計 MAX17048 は使えない。** I2Cアドレスが **0x36 固定で変更できず、つまみの
AS5600（0x36 固定）と正面衝突する。**

| 系統 | 部品 | I2Cアドレス | 判定 |
|---|---|---|---|
| 電圧だけ | ADS1115 | 0x48・空き | ❌ 消費電流が測れず、目的の半分が残る |
| **電圧＋電流** | **INA226** | **0x40〜0x4F から選べる・空き** | ⭐ **採用** |
| 残量% | MAX17048 | 0x36 ＝ AS5600と衝突 | ❌ 不可 |
| 残量% | LC709203F | 0x0B・空き | △ IC が生産終了扱いで入手が読めない |

✅ **実機の I2C アドレスは `0x44`**（2026-09-02）。`0x40` ではない。電流計の I2C 4 本を抜くと `0x44` だけが消え、挿すと戻ることで確かめた。読むコードはこのアドレスで書く。

**読むコードは 2026-09-11 に書いた**（[Battery.cpp](../firmware/esp32/src/Battery.cpp)。シリアル `bat`・`batlog <秒>`）。
同日 12:51 に OTA で焼いた。電池駆動（ユーザー確認）で、INA226 は型番 ID が合い、電池の電圧は 4.02〜4.05V と読めた。
🔴 **ところが電流は 2.5〜2.8mA から動かなかった。**同じ 1 分の中で、つまみの ON→OFF でリレーのコイル（約 30mA）と
画面が切れたのに、値は変わっていない。機体の電流（見積もり 200〜300mA・10mΩ で 2〜3mV）が、INA226 が読んでいる
シャントを通っていない。4 つの端子のどれがシャントの両端かは図面読みのままで（下の「電源の 4 穴の極性」）、
DT10B では 10mΩ と 0Ω を区別できないので、現物ではまだ確かめられていない。配線はシルク印刷どおり（同日 ユーザー）。
同日 16:24、電池が切れた後につまみ ON のまま PowerBoost に充電器を挿して 1 分読んだ。電池の電圧は
3.446V → 3.556V と上がり続けた（充電されている・INA226 はその電池の電圧を読んでいる）が、電流は 1.2〜1.8mA のまま
だった。**放電でも充電でも、電池と PowerBoost のあいだの電流は INA226 が測っている 10mΩ を通っていない。**
この 2 回の測定で付いていたのは、同日未明に煙を出した個体である（ユーザー）。I2C・型番 ID・設定の読み返し・電池電圧は
生きていて、電流だけが読めていない。新品に替えて同じ配線で読めば、個体の故障か通り道の側かが決まる。
その後、ユーザーがこの個体の **INA226 のパッケージの中央が焦げている**のを見つけた（同日）。電流が読めないことと
同じ向きの観察なので、この個体は電流計としては使わない。新品に替えて `bat` で電流が動けば確定。
✅ **同日 17:20 に新品へ替えると、電流が読めた。**煙の個体は電流の側が壊れていた（確定）。
新品のこの配線では電流の向きが反転していた（充電器を挿していないのに「充電 300mA」と出て、電圧は下がり続けた）ので、
Wi-Fi モニタから `batflip` を送った（NVS に保存）。
✅ **現行機が電池から引く電流の初めての実測**（同日 17:25・起きていて会話していない・画面は顔・無線は接続中）:
電池 3.38〜3.40V で **放電 300〜325mA（約 1.06W）**、2 秒ごとの最大は 355〜365mA。上の見積もり（待機 200〜300mA は 5V 側）と桁が合う。
続けて充電器を挿すと（機体は動かしたまま・PowerBoost の黄色の LED が点灯）、**充電 505〜560mA** で電圧は 3.727V → 3.741V と上がった。
`batflip` 後の向きは放電・充電とも正しい。
（同日 ユーザー: PD 充電器では充電が始まらず、別の充電器で充電した。20:2x にもう一度、Anker PowerCore PD 10000 では始まらず、
電池式の充電器では始まった。Type-C の充電器は受け側の CC に 5.1kΩ が無いと 5V を出さない。Type-C 基板は CC の 5.1kΩ×2 を
自分で付ける部品である（[typec_cc.html](../hardware/parts/typec_cc.html)）。どの口から充電したかはこの記録に無い。）
20:20 に電池の出口が 0.000V だった（使い切って保護が切ったと見られる）が、充電器を挿すと 20:26 には 3.58V・充電 480mA で戻った。

✅ **待機スリープの実測（同日 18:06〜18:12・電池 3.77〜3.78V・充電器なし）**。眠っている間は無線が止まって読めないので、
ファームが数え続ける積算の放電 mAh を眠る前後で読み比べた。

| 状態 | 電流 | 出どころ |
|---|---|---|
| 起きている・会話していない | **約 280mA**（2 秒平均 276〜287・1.06W） | `batlog 2` の 10 秒 |
| 待機スリープ（画面・無線・リレーのコイルを止める） | **約 160mA** | 18:06:28〜18:12:01 の 333 秒で 16.0mAh。うち眠っていたのは 18:06:51〜18:11:51 の 300 秒（ユーザーが画面で見た時刻）。前後の起きていた 33 秒を 280mA で引いて 13.4mAh ÷ 300 秒 ＝ 161mA（起きていた部分の見積もりの幅で ±10mA） |

眠ると約 120mA（4 割）減る。残る 160mA は眠らせていない部分で、内訳は測っていない。
つまみで起こすと 10 秒で Wi-Fi とモニタが戻った。
電流の向きもまだ確かめていないので、USB を挿さずに電池で動かして
「充電」と出たら `batflip` で反転する（NVS に残る）。残量の % は、電池の内部抵抗を 150mΩ と仮定して電流ぶんの降下を足し戻し、
リポの一般的な放電曲線に当てた目安であって、この電池で測った曲線ではない。

✅ **同じスキャンに出る `0x42` は ReSpeaker の XMOS 本体**（`firmware/xmosver/src` に `kXmosAddr = 0x42`。リソースID `0xF0`・コマンド `0xD8` で版数が読める）。⚠ **起動直後の自動スキャンには出ない。** XMOS の起動が ESP32 より遅く、間に合っていないだけで、しばらく経ってから `s` を撃つと出る。

✅ **着荷済み（2026-08-24）。Amazon の INA226 モジュール `B0GDX2P81Y`（ネジ端子・¥893）。
現物の刻印は `R010` ＝ 10mΩ で確定**（[TODO.md](TODO.md) A-2）。決め手はネジ端子で、電流の経路に半田付けが要らないこと。

### 挿す場所は「電池と PowerBoost のあいだ」

**リポ電池と PowerBoost の電池入力のあいだに、直列に入れる。**

理由は、INA226 が読む「バス電圧」が**そこに入れたときだけ電池電圧そのものになる**からである。
5V の側（PowerBoost の出力）に入れると電流は測れるが、読める電圧は 5V レールなので電池の減り具合が
分からず、「電池残量」が別問題として残ってしまう。電池側に入れれば、**消費電流の実測と電池残量が
本当に1個の部品で片付く。** 充電中は電流が逆向きになるが、INA226 は符号付きで読めるので、
充電中か放電中かもそのまま分かる。

### 🔒 電源の 4 穴の極性は「内側 2 本が GND（−）・外側 2 本が +」

✅ **2026-09-10 ユーザー実測。** 5 ピンヘッダの `GND` ピンと大穴の導通で判定した（無通電。ヘッダのシルク
`VCC/GND/SDA/SCL/ALER` が唯一の確実な基準で、大穴側のシルクは読まなくてよい）。ヘッダと反対の短辺に
4 穴が一列に並び、**内側の 2 穴が − ・外側の 2 穴が +** である。

⚠ どちらの端が INPUT でどちらが OUT かは図面読みのまま（[parts.scad](../hardware/parts/parts.scad)
`ina_pwr_pins()`: `y < 10` が INPUT 側）。この並びが正なら 端から INPUT+ / INPUT− / OUT− / OUT+ になる。

🔴 **守るのは + と − だけ。INPUT と OUT は取り違えても壊れない。** どちらも同じシャントの両端なので、
逆に入れれば電流の符号が反転して読めるだけである（INA226 は双方向）。**+ と − を逆にすると、その先の
PowerBoost に逆電圧がかかる。**

✅ **線の割り込み方は JST の延長ケーブル**（2026-09-10 ユーザー「電池から、延長を繋いで、電流計の IN に
入れたい」）。電池付属のケーブルは切らず、延長を1本足してそちらを切って割り込ませる（失敗しても延長
ケーブルを捨てるだけで済む）。⚠ 延長は赤黒がハウジングの向きで逆に出ることがあるので、切る前に電池を
挿した状態で先端の電圧を測って + を確かめる（剥いた 2 本を触れさせないこと）。

### 🔴 音量の上限はソフトではなくアナログ側に置く

🔴 **この節と、下の「接点に流れる電流 0.5A rms」の両方が、「スピーカーは 8Ω 2W」という
出どころの無い数字の上に乗っている**（2026-09-13 に判明）。

    `docs/DIMENSIONS.md` の秋月 [112495] の行には **⬜ 定格(W)** と書いてある（未確認）
    2026-09-13、商品ページを取り直した ⇒ **インピーダンス 8Ω は書いてあるが、定格(W) の記載が無い**
    ⇒ 🔒 **2W はどこから来たのか辿れない。**この文書の 5 か所で「定格 2W」として使われている

⚠ **2W が違っていたら動く物:** ①接点に流れる電流（1W なら 0.35A・2W なら 0.5A・5W なら 0.79A rms）
②ソフトの音量の上限 ③スピーカー自身が壊れるか。
⭐ **①は、どの値でも接点定格 1A の内側**なので**接点の判断は変わらない**。
🔴 **②③は変わる。**⇒ **音量の上限は、定格が分かるまで「実物で耳を離して決める」以外に根拠が無い。**
⇒ **調停役へ回してある**（2026-09-13。取れなければファーム側に上限を入れる話として引き取る、との返答）。

肩＝耳道から数cmで、8Ω 2W に一般的な感度を仮定すると 10cm で 105〜110dB SPL 台の概算になる。
基板側のアンプは5Wまで対応（Seeed wiki）で、⚠ **スピーカーの定格（2W と仮定）**より大きいので、ソフトの音量
（既定35% = `gain_`）を上げすぎるとスピーカー側の定格を超えられる。

⚠ **既定の35%は3.5mmジャックにイヤホンを挿して決めた値である。** スピーカーを繋いだら音量は
取り直しになる。**最初は35%より下げてから鳴らし、耳から離した状態で確かめること。**

---

## 7. 🔒 却下した案（蒸し返さないこと）

| 案 | なぜ却下したか |
|---|---|
| **減衰器**（スピーカー線に固定の抵抗分圧） | フルスケールが安全な音圧に収まる減衰量まで削ると、肝心の声が聞こえなくなる。必要なのは音量を下げることではなく**開閉すること** |
| **ラッチングリレー** | 消費電流では有利だが、電源が落ちても閉じたまま保持する＝**防ぎたい状態そのもの** |
| **eFuse `UART_PRINT_CONTROL=3` でROMブートログを消す** | リレーが開いていればブート窓は音源ごと無音になるので、**不可逆な操作をする理由が無い** |
| **手動の物理スイッチでミュート** | 人が操作しないと効かない。**装着者にその判断は求められない** |
| **2.54mm ピンヘッダ＋DuPont で スピーカー IN/OUT を作る** | 🔴 **AI が「PH2.0 が無くても在庫で組める」と提案して却下された。** ① 8Ω 2W で **0.5A rms** が流れて信号線の桁ではない ② **DuPont の接触抵抗が 8Ω と直列に入る**（個体差も大きい） ③ DuPont は緩む前提の接続で、鳴っている最中に接触が暴れればノイズになる ④ 同じ板に I2C が通り **ReSpeaker のマイクが数cm** の距離にある |
| **5Vを作らずに 3.7V→3.3V の降圧だけで済ませる** | ① PowerBoost はすでに手元にあり実機で通っていて、**いま採れる選択肢ではない** ② **アンプはたぶん5Vで動いている**ので、3.3V を直結しても音が出ない（か極端に小さい）。**⚠ 第2世代で自前基板の電源ICを選び直すときには、また検討の余地がある** |
| **PowerBoost のパッドに線2本を直接半田付け** | ストレインリリーフが要る・不可逆 |
| **XIAO の充電回路を使う** | 1章のとおり充電電流 100mA では追いつかない |
| **MAX17048 / ADS1115 で電池残量** | 6章のとおり。アドレス衝突と、電流が測れない |

⚠ **「細いこて先が無い」は、スルーホールへのピンヘッダ付けを否定しない。**
旧 SHOPPING.md（2026-08-24 削除・git 履歴） のあの条件が指しているのは**表面実装の重ね付け**であって、**2.54mm の
スルーホールにピンヘッダーを立てる作業は対象外**である。**制約を広く読み過ぎて、成立する案を
消さないこと。**
